# Batch 13: the open-source batch

Date: 2026-09-03. Status: approved by the owner on 2026-09-03, as written.
Driver: backlog batch 13 — HR-046 (batch 12 branch review), HR-045
(owner request). Branch `batch-13` from `6ff7bcc`.
Process: implementer on sonnet, task review on opus, branch review on
fable; one tier up after two endpoint drops; strictly sequential; the
audit `--ids` on every round are the dispatch's Knowledge ids.
Workspace `.superpowers/sdd/2026-09-03-batch-13/`.

## 1. Goal

The public repository presents itself: a README written for a
first-time visitor, the standard open-source files, the repository
settings — and the release loop closes with the restamp step in the
skill that drives every merge.

## 2. Out of scope

- Any npm publish machinery (decision 3's npm part); any change to
  release-please config; HR-041/042/043.
- No dependency; the payload stays on built-ins.
- The kit's seeded template files: this batch documents THE REPOSITORY;
  nothing under `template/` changes except the finishing-a-feature
  skill (T1).

## 3. Facts

- HR-044 happened because no loading path put the restamp step in
  front of the agent doing a release merge; the batch 12 review ruled
  the fix a skill step, not a standing rule.
- The repository is public at github.com/jblossey/houserules with the
  0.2.0-alpha pre-release; CI and release-please run on push. The
  current README opens with the kit description but was written for
  the dogfooding repository, not a visitor.
- LICENSE (MIT) exists; no CONTRIBUTING, SECURITY, code of conduct,
  or issue/PR templates exist; the repository has no description or
  topics set.

## 4. Tasks, in order, one Conventional Commit each

### T1 `fix(template): finishing-a-feature carries the release restamp step` (HR-046)

- One step added to
  `template/.claude/skills/finishing-a-feature/SKILL.md`: after
  merging a release-please PR, run
  `mise exec -- node bin/houserules.mjs update --dir .`, commit
  `chore(release): restamp the kit version`, push
  (`houserules.post-release-restamp`, `docs/runbook.md`). Generic
  wording apart from those two pointers — the kit ships this skill,
  and the pointers name kit-owned artifacts every install has (the
  entry is root-only here, so phrase the pointer as the file and the
  step, not the repo-only id — check what a fresh install actually
  carries and phrase accordingly; state the check in the report).
- Root sync via `update --dir .`, byte-identical. No scenario covers
  the skill: no evals rerun. Data-only; gates as evidence.

### T2 `docs(oss): the README and the open-source files` (HR-045)

- README rewrite for a first-time visitor: what the kit is and why,
  install (`pnpm add -D houserules` once published — until then the
  git install form that works today, verified live), quick start
  (init, the generated files, the first knowledge entry), the
  ownership model (KIT_OWNED / SEED_ONCE / yours), the agent workflow
  (implementer, task-reviewer, branch-reviewer, the audit), updating
  (the existing drift-line section stays), development (mise/pnpm,
  tests), license. CI badge; release badge. Every claim verified
  against the source (`knowledge-base.state-only-the-source`); every
  command run live or marked as post-publish.
- CONTRIBUTING.md: setup (mise, pnpm), the batch process in brief
  (backlog, spec gate, sequential agents, reviews), Conventional
  Commits with the 100-char limits, the no-trailer rule, how the
  commit-msg hook enforces both, tests and gates before a PR.
- SECURITY.md (private reporting via GitHub security advisories);
  CODE_OF_CONDUCT.md (Contributor Covenant, current version, verified
  against its source); `.github/ISSUE_TEMPLATE/` (bug report, feature
  request) and `.github/PULL_REQUEST_TEMPLATE.md` (checklist matching
  the real gates).
- Repository settings via `gh api`: description and topics (e.g.
  knowledge-base, claude, agents, ai-workflow, conventions —
  final list in the report), homepage unset until docs exist. Record
  the commands and responses as live evidence.
- Data-only for the suite; gates as evidence; the README's install
  command exercised live in a scratch directory.

## 5. Reviews and finish

Per batch: task review (opus) with the full block on every round;
every finding fixed or a reasoned backlog item; branch review (fable)
with `--workspace`; retrospective applied; ticks; one to five clean
commits; ff-only merge from the CLI; push; CI green on main.
