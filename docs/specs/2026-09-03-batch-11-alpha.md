# Batch 11: the alpha batch — capture form, update path, release groundwork

Date: 2026-09-03. Status: approved by the owner on 2026-09-03, as written.
Driver: backlog batch 11 — HR-040 (batch 10 branch review), HR-038,
HR-039 (owner ruling, design.md §5.19). Branch `batch-11` from
`fc445a6`. Merging this batch constitutes alpha state (owner gate,
2026-09-03); the upstream creation follows immediately after the merge
as an owner-attended step, not a batch task.
Process: implementer on sonnet, task review on opus, branch review on
fable; one tier up after two endpoint drops; strictly sequential; the
audit `--ids` on every round are the dispatch's Knowledge ids. Workspace
`.superpowers/sdd/2026-09-03-batch-11/` with the ledger `progress.md`.

## 1. Goal

Reach alpha state: the evidence-capture form and the collision line
become decidable template text, adopters get a working update path over
the existing version stamp, and the release-please groundwork lands
ready to activate the moment the upstream exists.

## 2. Out of scope

- Creating the upstream, `gh repo create`, any push, any npm publish —
  these follow the merge, with the owner, per design.md §5.19.
- Any dependency added to the payload; `template/` stays on built-ins.
- Repo name, visibility, and org for the upstream: owner inputs at the
  post-merge step.

## 3. Facts

- Batch 10 produced three evidence-capture findings of one class and
  one undecidable eval line; HR-040 pairs both fixes so one evals rerun
  pays them (the branch review's template_defects 1 and 2).
- `init` and `update` already stamp `{ version, idPrefix }` into
  `.houserules.json` (bin/houserules.mjs writes the marker), but
  nothing reads the version back: `update` is silent about what it
  upgrades from and to, and no doc states the adopter flow (HR-038).
- The owner ruled (design.md §5.19): release-please, changelogs from
  the conventional commits, first release a pre-release under the
  alpha dist-tag, upstream right after alpha state. Local groundwork
  can precede the upstream; the automation activates on GitHub.
- `implementer.md` and `.claude/evals/*.json` changes trigger
  process.evals-rerun; T1 triggers and T4 pays with the sixth record.
  The scenario copies are SEED_ONCE and pinned byte-exact by the
  dogfood suite, so the root copy syncs with the template edit.

## 4. Tasks, in order, one Conventional Commit each

### T1 `fix(template): the capture form and the decidable collision line` (HR-040)

- `template/.claude/agents/implementer.md`, the `live_run` bullet: ONE
  sentence naming the canonical capture form — one command per entry,
  captured as `<command> > <file> 2>&1`, the recorded exit is that
  command's own, no `;` or `|` tails. No restructuring. Root synced via
  `update --dir .`.
- `template/.claude/evals/dependency-add.json`, expected_behavior[1]:
  reword to the two decidable outcomes — with a payload manifest
  present, place the dependency outside it and disclose the collision
  in `concerns`; with the root manifest as the only manifest, keep the
  payload import-free and state the placement reasoning in the report.
  Root copy synced byte-exact (the scenario pin test enforces it).
  Check the sentence against every run shape it must not capture
  (writing-style.instructions-cover-the-state-space).
- Data-only: `tdd` empty with the disclosure; gates in `tests`.
  Expected-red co-change until T4, disclosed in concerns.

### T2 `feat(cli): update reports the kit version drift` (HR-038)

- `update` prints one line naming the stamped version it found and the
  running version it syncs to (e.g. `kit 0.1.0 -> 0.2.0`), from the
  existing marker read; unchanged-version runs say so in the same
  line's shape. No new command, no new flag (YAGNI).
- README gains the adopter update flow: bump the houserules dependency,
  run `update --dir .`, review the diff; the drift line is the check.
- TDD: natural RED first for the drift line (stamp an older version in
  a scratch install, assert the printed line); full suite and lint
  green. Live run: the corrupt-and-restore recipe over a scratch
  install with a doctored stamp version.

### T3 `chore(release): release-please local groundwork` (HR-039)

- Add the release-please configuration for a single-package node repo
  with the prerelease strategy producing `-alpha` versions, the
  manifest file seeded from the current package.json version, and the
  GitHub workflow file pinned by exact action version — inert until an
  upstream exists. Verify every config key and the action reference
  against current release-please docs (context7); record the expected
  first computed version in the report.
- No dependency enters package.json; no publish configuration beyond
  what the workflow needs. Data-only in effect (nothing executes
  locally): gates as evidence; a config-lint or schema check counts as
  live evidence where the tooling offers one.
- RULED by the owner 2026-09-03 (T3 review, finding 1): the first
  alpha changelog covers batch 11 — `bootstrap-sha` is `fc445a6`, the
  batch 11 gate commit on main. An in-branch sha dies at commit
  aggregation; fc445a6 survives, and the first computed release
  (expected 0.2.0-alpha once the batch's feat aggregates) documents
  the alpha batch itself.

### T4 `docs(evals): sixth evaluation record` (process.evals-rerun)

- After T3: every scenario in a detached scratch worktree at the branch
  head per process.eval-fixture-procedure; the reworded collision line
  judged decidably this time; append the run set keyed by both template
  blob ids; worktree removed; `eval-` artifacts in the batch workspace.

## 5. Reviews and finish

Per batch: task review (opus) with the full
`Knowledge:`/`Backlog:`/`--ids` block on every round; every finding
fixed or a reasoned backlog item; branch review (fable) with
`--workspace`; retrospective applied; ticks; one to five clean
commits; commitlint and the audit over the range; CLI ff-only merge.
Every controller slip logged when it happens, gate-caught or not.
After the merge: report alpha state to the owner and stage the
upstream-creation steps for their go.

## 6. Code-health scan (files this batch touches)

- `template/.claude/agents/implementer.md` and the dependency-add
  scenario: one sentence and one line reworded; no restructuring.
- `bin/houserules.mjs`: the drift line reuses the existing marker read;
  no second stamp reader appears (DRY).
- README: one new section in the existing voice.
- Release config files: new, minimal, every version reference exact.
