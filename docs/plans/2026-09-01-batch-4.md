# Plan: batch 4 — the evals-rerun port, and polish

Spec: `docs/specs/2026-09-01-batch-4-evals-port-and-polish.md` (approved
2026-09-01, T5 included). Branch `batch-4`; base of the batch `4043d75`.
Workspace `.superpowers/sdd/2026-09-01-batch-4/` (ledger `progress.md`,
briefs `task-N-brief.md`, reports, reviews, audits). Every task: one
Conventional Commit (the hook and the audit both cap header and body lines
at 100 characters; identifiers stay unsplit; lowercase subject; no trailer),
TDD per `process.tdd` (a disclosed-mutation proof where behavior is already
correct), JSON report at `REPORT_FILE`, then task review (opus); the
controller evaluation run (T5) after Task 4 clears; branch review (fable)
with `--workspace` after T5.

Commands run under mise: `mise run test`, `mise run lint`, `mise exec --
pnpm exec vitest run tests/<file>.test.mjs --coverage.enabled=false` for a
focused run, `mise exec -- node bin/houserules.mjs update --dir .` to sync
root copies. Never `npm` or `npx`. Scratch repositories live under the
session scratchpad and run tools as `mise x node@24.18.1 -- <command>`.
~/projects/tag-pilot is a read-only reference: Task 4 reads its
`knowledge/process.json`, `tagpilot-orchestrating/SKILL.md`, and
`tagpilot-branch-reviewer.md` for the verbatim source text; nothing there is
modified or run.

Two-copy files this batch: `knowledge/process.json` and
`template/knowledge/process.json` (Task 3 touches the seed only — the root
already carries the brief rule; Task 4 touches both), edited by targeted
text insertions that keep each file's layout; `git diff --stat` shows a few
lines per file. Kit-owned files (`template/tools/kb.mjs`,
`template/.claude/agents/branch-reviewer.md`,
`template/.claude/skills/orchestrating/SKILL.md`) are edited under
`template/` and synced with `update --dir .`.

## Code-health scan

See the spec §6: the stamp read is the last raw `JSON.parse` of project
data in the bin (T1); `gitDiff`'s catch swallows the failure cause (T2,
export with JSDoc); the skill and agent grow one section, two bullets, one
sentence; no new fixture duplication is expected.

## Pre-flight

- T1: rewriting the stamp-pin test removes the only cover of `main`'s
  non-UsageError rethrow branch in `bin/houserules.mjs` — the brief demands
  a replacement cover (a real path or a disclosed mock) and the coverage
  gate (lines 80 / branches 99; 98.73 / 99.2 at base) stays green.
- T3 and T4 both edit `template/.claude/skills/orchestrating/SKILL.md`,
  sequentially; no conflict.
- T4 changes the ownership lists: the `files` manifest test and the init
  seed-count assertions move (RED first).
- T4 verifies (and reports) that the `docs` area of both `areas.json`
  copies routes `.claude/**`, so `process.evals-rerun` rides every
  template-touching audit without `--ids`.
- T5 runs on templates loaded at session start: the controller's eval
  dispatches carry the scenario protocol inline (the very bullet T3 adds).
- The co-change check activates for this repository once the root
  `.claude/evals/record.json` exists (T5 writes it); T4's own commit
  changes `.claude/evals/` in the template only, not the root, so the batch
  audit does not trip the new rule before T5.

### Task 1: an unreadable stamp file is one usage error (HR-010)

**Files:**
- Modify: `bin/houserules.mjs` (the stamp read in `install`),
  `tests/init.test.mjs`

**Behavior:** `install` reads an existing `.houserules.json` with `readJson`
(already exported by `template/tools/lib/json-store.mjs`; the bin imports
`emit` from there). Invalid JSON in the stamp becomes
`UsageError('<path>: invalid JSON (...)')`: one stderr line, exit 2, no
stack trace. A missing stamp keeps today's default path.

**Steps:**
- [ ] Step 1, RED: rewrite "propagates an unreadable marker, which is a
  defect and not a usage error" into "reports an unreadable stamp as one
  usage error": `main(['update', '--dir', dir], io)` (after a clean init,
  with `printf '{' >` over the stamp) returns 2; `io.stderr` names
  `.houserules.json`, matches `/invalid JSON/`, has one line, no `    at `.
  Focused run → RED.
- [ ] Step 2: implement (import `readJson` beside `emit`); focused GREEN.
- [ ] Step 3: keep the rethrow branch covered: add or adapt a test that
  drives a non-UsageError through `main` (a real defect path if one exists;
  otherwise a disclosed `vi.mock`-based one in the existing
  `node:child_process` factory style — state the choice and the reasoning
  in `self_review`). Full `mise run test` meets the coverage gate; record
  the numbers.
- [ ] Step 4, live run (scratch, `mise x node@24.18.1 --`): `init` ok with
  both checks; corrupt `.houserules.json`; `init --dir <scratch>` → one
  line naming the stamp, `EXIT: 2` (own entry with `exit`); restore (rewrite
  the stamp as `{"version":"0.1.0","idPrefix":"WI"}` or re-init a fresh
  scratch); ok.
- [ ] Step 5: commit `fix(cli): report an unreadable stamp file as one usage
  error`; report; validate; audit `--base <BASE> --head HEAD`; self-review.

### Task 2: the diff failure carries git's stderr (HR-011)

**Files:**
- Modify: `template/tools/kb.mjs` (`gitDiff`, exported with JSDoc),
  `tests/kb.test.mjs`
- Synced by `update --dir .`: `tools/kb.mjs`

**Behavior:** `gitDiff` pipes the child's stderr
(`stdio: ['ignore', 'pipe', 'pipe']`). On failure: when the stderr contains
`no merge base` (or `merge base`), the message stays exactly
`no merge base between "<base>" and "<head>"`; otherwise the `UsageError`
carries the stderr's first non-empty line, trimmed (fallback: the error's
own message). Export `gitDiff`; the JSDoc names the two message sources and
the caller's range obligation.

**Steps:**
- [ ] Step 1, RED: the orphan-branch test stays byte-identical; a new unit
  test calls `gitDiff(root, base, head, ['--name-only', '<bad input>'])`
  with an invalid pathspec (e.g. `':(bad'`) and expects a `UsageError`
  whose message starts with git's own `fatal:` line, not the merge-base
  text. Focused run → RED (today it says `no merge base …`).
- [ ] Step 2: implement; `update --dir .`; focused GREEN; full suite meets
  the coverage gate.
- [ ] Step 3, live run: the scratch orphan audit prints the unchanged
  message with `EXIT: 2`; the invalid-pathspec case (through a direct
  `node` call or the unit) shows git's own first line.
- [ ] Step 4: commit `fix(tools): carry the git stderr in the diff failure`;
  report; validate; audit; self-review.

### Task 3: dispatch-protocol bullets and the brief rule in the seed (HR-012)

**Files:**
- Modify: `template/.claude/skills/orchestrating/SKILL.md` (dispatch
  protocol, two bullets), `template/knowledge/process.json` (the
  `process.brief-carries-the-spec` entry, a targeted insertion of the root
  entry's exact object before `process.brainstorm-first`)
- Synced by `update --dir .`: `.claude/skills/orchestrating/SKILL.md`

**Bullet texts (verbatim):**
- "A brief names every test, gate, and file its spec task lists, verbatim
  or by pointer (`process.brief-carries-the-spec`); an implementer who
  cannot satisfy one flags it instead of dropping it."
- "When a batch edits an agent template or skill, the dispatch message
  carries the changed instruction verbatim: templates load at session
  start, so the running session's copy is stale until a restart."

**Steps:**
- [ ] Step 1: the seed insertion (layout kept; `git diff --stat` a few
  lines); the two bullets; `update --dir .`; `tools/kb.sh render` (no
  generated change expected at the root — the root entry already exists).
- [ ] Step 2, evidence: the self-maintaining seed `checkBase` test stays
  green (it reads the directory); full `mise run test`; `mise run lint`.
  Live run (scratch): `init` seeds the entry (`<scratch>/tools/kb.sh get
  process.brief-carries-the-spec` prints it) and both checks pass. TDD note:
  prose plus a seed copy of an existing entry — the seed test is the
  regression net; no natural RED exists; say so in `tdd`/`self_review`.
- [ ] Step 3: commit `docs(template): dispatch-protocol bullets and the
  brief rule in the seed`; report; validate; audit; self-review.

### Task 4: the evals-rerun rule, record seed, and template sections (HR-003)

**Files:**
- Modify: `template/knowledge/process.json` and `knowledge/process.json`
  (the `process.evals-rerun` entry in both, targeted insertions),
  `bin/houserules.mjs` (`SEED_ONCE` gains `.claude/evals/record.json`),
  `template/.claude/skills/orchestrating/SKILL.md` ("Template evaluation"
  section before "## Rulings"), `template/.claude/agents/branch-reviewer.md`
  (the blob-id sentence appended to the rule-adherence paragraph),
  `tests/init.test.mjs` (ownership-list and seed assertions),
  `tests/kb.test.mjs` if the seed test's fixture needs the new file
- Create: `template/.claude/evals/record.json` (`[]`)
- Synced by `update --dir .`: the skill and agent root copies

**Source texts:** read `~/projects/tag-pilot/knowledge/process.json`
(`process.evals-rerun`), the "Template evaluation" section of
`~/projects/tag-pilot/.claude/skills/tagpilot-orchestrating/SKILL.md`, and
the record sentence in
`~/projects/tag-pilot/.claude/agents/tagpilot-branch-reviewer.md` (line 22).
Port verbatim except the rename map (`houserules.rename-map`):
`tagpilot-implementer` → `implementer`, `tagpilot-task-reviewer` →
`task-reviewer`, batch references generalized. Keep the `check` object's
shape exactly. Do not port `rust-test-near-coverage`.

**Steps:**
- [ ] Step 1, RED: the `files` manifest test and the init seed assertions
  expect `.claude/evals/record.json` in `SEED_ONCE` and on disk after init
  → RED before the list and seed exist.
- [ ] Step 2: implement all files; `update --dir .`; `tools/kb.sh render`
  (the new entry is non-standing, area `docs`); focused GREEN; full suite;
  lint.
- [ ] Step 3: verify and report: the `docs` area of `knowledge/areas.json`
  and `template/knowledge/areas.json` includes `.claude/**`.
- [ ] Step 4, live runs (scratch): `init` seeds `record.json` as `[]` and
  both checks pass; in the same scratch, change `.claude/agents/
  implementer.md` and commit without touching the record → `tools/kb.sh
  audit --base HEAD~1` fails the `process.evals-rerun` co-change row; also
  change the record in a second commit → the audit passes. Record each run
  verbatim with `exit`.
- [ ] Step 5: commit `feat(template): the evals-rerun rule with its co-change
  check and record`; report; validate; audit; self-review.

### Task 5 (controller): first evaluation run

After Task 4's review clears: create a detached scratch worktree at the
branch head; run `docs-edit` and `dependency-add` through the `implementer`
template (sonnet; the scenario's `query` is the brief, its `knowledge` ids
the `Knowledge:` line) and `seeded-violations` through the `task-reviewer`
template (opus, fixture from its `setup`); judge every `expected_behavior`
line from the report or review; append the first run set to the root
`.claude/evals/record.json` (`date`, `templates`: `git rev-parse
HEAD:.claude/agents/implementer.md` and `...task-reviewer.md`, `runs` with
`scenario`, `agent`, `model`, `pass`, `of`, `notes`); commit `docs(evals):
first evaluation record`. Workspace artifacts carry the `eval-` prefix;
keep nothing from the worktree. Then the branch review.
