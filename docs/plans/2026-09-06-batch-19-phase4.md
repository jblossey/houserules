# Plan: batch 19 — derived from the approved phase-4 spec

Written 2026-09-06 AFTER the spec's approval (spec-then-plan,
design.md §5.32). Spec: docs/specs/2026-09-06-batch-19-phase4.md
(approved 2026-09-06; cargo-dist and macOS-unsigned ruled at the
gate, §5.37). Items: HR-048, HR-049, HR-063, HR-064. HR-066 is
NOT gated in (spec §6).

Branch `batch-19` (already carries the batch-18 report rulings
and this gate's records). Workspace
`.superpowers/sdd/2026-09-06-batch-19/` (ledger progress.md).
Process per the standing set: sequential sonnet implementers,
opus task reviews, fable branch review; audits WITH --report via
the binary; the checker closing act; claims-match now covers
shipped doc sentences (§5.33); TDD mode labels per §5.34;
aggregation with explicit path staging + tree identity; the
standing rebase rule at the finish.

## Task derivation from the spec

### T1 — the cargo-dist pipeline (spec §2, §3)

Adopt cargo-dist per the ruling: the deep vet (maintenance,
releases, ownership, the 2025 axo turbulence and its resolution)
recorded in dependency_vetting; the tool version pinned exactly
everywhere it appears (the dist config's own version field and
the generated workflow's pin — verify how 0.32 pins itself and
record it). `dist init` against the five ruled targets
(linux x64/arm64, macOS x64/arm64, Windows x64), sha256
checksums, the shell + powershell installers, ubi-compatible
archive names; the generated .github/workflows/release.yml is
GENERATED — its regeneration discipline documented (edit the
config, re-run dist, never hand-edit the output; a header
comment states it). Trigger: the release-please `v*` tag — the
include-component-in-tag flip to false lands here (the §5.20
flip, this batch's first config-touching task) with the alias
tag reality stated. macOS ships unsigned (§3 ruling) — no
signing steps enter the workflow. Live proof (the spec §7
two-step reality, step one): `dist plan` clean; `dist build`
for the host target producing a real archive with the expected
name and checksum; the workflow's syntax/shape validated in CI
by this PR's own checks; the dry-run mechanism against a draft
release settles HERE and its choice is documented (
workflow_dispatch with a draft flag if cargo-dist 0.32 supports
it — the implementer verifies against current docs and states
what is possible).

### T2 — the consumers flip (spec §4, §5; HR-049/063/064)

After T1 fixes the channel shapes (asset names, installer URL
grammar): README's build-from-source section retires (HR-064) —
the install blocks become the real channels (the curl-to-sh
installer form, the `mise use ubi:jblossey/houserules` form,
the direct-download + quarantine note for macOS), each block
walked per the §5.33/state-only-the-source method — where a
block cannot run until the first release exists, it is marked
pending that release IN THE BLOCK and the two-step note names
the re-verification; the batch does not claim a walk it cannot
perform (the honest two-step reality, spec §7). HR-049: measure
release-please's extra-files annotation support against the
README pin shape; wire the pins there if the shape fits, else
the runbook restamp step gains the pin-update line — the
measured decision recorded. HR-063: template/.github/workflows/
knowledge.yml gains its install step consuming the channel
(pinned version; the step's exact form defended against the
alternatives — the installer script vs ubi via mise-action);
the payload invariant's first known-gap bullet (HR-063's)
retires from BOTH knowledge copies; render. The runbook's
release section documents the whole flow (tag → binaries →
pins → restamp) and each owner-attended external act (mise
registry short-name PR, tap, asdf plugin, Scoop/winget) as a
checklist the owner can execute.

## Code-health scan (the files this batch touches)

- .github/workflows/: release-please wiring untouched except
  the config flip; the cargo-dist release.yml is generated
  output — the scan's one rule is never hand-edit it (the
  regeneration discipline is T1's deliverable).
- release-please config: the include-component-in-tag flip is
  a one-key change; verify the config file's real name/shape
  before editing (measure, not assume).
- README.md: one temporary section to retire (HR-064); the
  ownership table stays consistent with `houserules files`.
- template/.github/workflows/knowledge.yml: 20 lines post-T6;
  gains one step; stays runnable-as-seeded per the invariant
  once the channel exists (until then the known-gap bullet
  transforms into the pending-first-release note, honestly).
- docs/runbook.md: the restamp section was rewritten at batch
  18's close; the release section grows around it — no
  duplication, the restamp step is referenced not repeated.
- knowledge/houserules.json: body-bullet edits only
  (ids-are-permanent).

## Verification (controller, after T2 — spec §7)

Step one of the two-step reality: dist plan/build captures, the
PR's own CI green on all three OSes, the seeded workflow's shape
validated, the README blocks walked where runnable and honestly
marked where not. Branch review (fable) with --workspace. The
finish per finishing-a-feature with the standing rebase rule,
aggregation with tree identity, PR, 3-OS CI, ff-only merge.
Step two (owner-attended): the first real `v*` release fires
the pipeline; HR-049/HR-063 ends re-verified live against it;
the acceptance record carries both steps.

## Dispatch data

- T1 Knowledge: security-hygiene.dependency-vetting,
  security-hygiene.exact-pins, security-hygiene.verify-current-
  docs, process.tdd, process.claims-match-artifacts,
  process.evidence-outlives-the-session,
  process.live-run-before-ci, houserules.dev-tools-are-rust-
  native, houserules.tests-clean-scratch-dirs.
- T2 Knowledge: knowledge-base.state-only-the-source,
  houserules.payload-runs-on-builtins,
  houserules.template-is-the-source,
  houserules.readme-mirrors-kit-owned, process.rulings-to-file,
  process.claims-match-artifacts,
  process.evidence-outlives-the-session,
  knowledge-base.ids-are-permanent.
- Models: implementer sonnet, task review opus, branch review
  fable; one tier up after two endpoint drops. Strictly
  sequential; T2 starts only after T1's review closes.
