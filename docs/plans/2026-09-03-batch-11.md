# Plan: batch 11 — the alpha batch

Spec: `docs/specs/2026-09-03-batch-11-alpha.md` (approved 2026-09-03,
as written). Branch `batch-11`; base `fc445a6`. Workspace
`.superpowers/sdd/2026-09-03-batch-11/` (ledger `progress.md`, briefs,
reports, reviews, audits). Every task: one Conventional Commit (the
hook gates both harness trailers and runs commitlint; header/body
lines at 100; lowercase subject start), TDD per `process.tdd` with
`mode` per the timing key, report per the full contract, task review
(opus), branch review (fable) with `--workspace` after T4. The audit
`--ids` on every round are the dispatch's Knowledge ids, verbatim.

Commands under mise; pnpm only. Scratch repositories under the session
scratchpad with `mise x node@24.18.1 --`; every capture `> file 2>&1`
per command with the recorded exit belonging to the recorded command;
sequences `&&`-chained; evidence brackets one chain in one call; every
controller slip logged when it happens. Never stage
docs/orchestrator-prompt.md. No remote, no push, no publish anywhere
in this batch.

## Code-health scan

Spec §6: one sentence in implementer.md's live_run bullet and one
reworded scenario line, no restructuring; the drift line reuses the
existing marker read in bin/houserules.mjs, no second stamp reader;
README section in the existing voice; release config files new,
minimal, every version reference exact.

## Pre-flight

- T1 touches implementer.md AND a scenario file: both trigger
  process.evals-rerun; T4 pays with the sixth record; T1-T3 carry the
  disclosed expected-red row in concerns. The scenario root copy is
  SEED_ONCE and byte-pinned: sync it with the template edit in the
  same commit.
- T2's drift line reads the marker `update` already loads — assert no
  duplicate read path. The unchanged-version line keeps the same shape
  (one line, both cases).
- T3 executes nothing locally: the workflow must be inert without a
  remote; the action reference and every version in the config are
  exact; the report records the expected first computed version
  (prerelease alpha) verified against current docs.
- T4 judges the reworded collision line decidably — the single-manifest
  branch of the new wording applies to this repository.

### Task 1: capture form + decidable collision line (HR-040)

Files: `template/.claude/agents/implementer.md` + root sync,
`template/.claude/evals/dependency-add.json` + root sync (byte-exact).
Data-only.

### Task 2: the update drift line (HR-038)

Files: `bin/houserules.mjs`, `tests/init.test.mjs` (or the update
tests' home), `README.md`. Natural RED first.

### Task 3: release-please groundwork (HR-039)

Files: release-please config + manifest, `.github/workflows/` release
workflow (exact-pinned action), CHANGELOG bootstrap per current docs.
Nothing runs locally; docs-verified evidence and gates.

### Task 4: sixth evaluation record

Controller-driven per process.eval-fixture-procedure; `docs(evals)`
commit keyed by both template blob ids at the branch head.

## Dispatch data

- T1 Knowledge: houserules.template-is-the-source, process.evals-rerun,
  process.deliverables-json, process.tdd, writing-style.principles,
  writing-style.instructions-cover-the-state-space, quality.principles,
  houserules.node-under-mise, knowledge-base.state-only-the-source.
- T2 Knowledge: houserules.template-is-the-source,
  houserules.live-run-recipe, houserules.node-under-mise, process.tdd,
  process.deliverables-json, security-hygiene.verify-current-docs,
  writing-style.doc-comments, writing-style.principles,
  quality.principles, quality.no-compat-softening.
- T3 Knowledge: security-hygiene.verify-current-docs,
  security-hygiene.exact-pins, process.tdd, process.deliverables-json,
  quality.principles, houserules.node-under-mise,
  houserules.payload-runs-on-builtins, writing-style.principles.
- Models: implementer sonnet, task review opus, branch review fable;
  one tier up after two endpoint drops. Strictly sequential.
