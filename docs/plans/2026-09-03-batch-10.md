# Plan: batch 10 — scratch-dir hygiene, status tie, ledger bullet

Spec: `docs/specs/2026-09-03-batch-10-hygiene-and-status.md`
(approved 2026-09-03, as written). Branch `batch-10`; base `35ac91b`.
Workspace `.superpowers/sdd/2026-09-03-batch-10/` (ledger `progress.md`,
briefs, reports, reviews, audits). Every task: one Conventional Commit
(the hook gates both harness trailers and runs commitlint; header/body
lines at 100; lowercase subject start), TDD per `process.tdd` with
`mode` per the timing key, report per the full contract, task review
(opus), branch review (fable) with `--workspace` after T4. The audit
`--ids` on every round are the dispatch's Knowledge ids, verbatim.

Commands under mise; pnpm only. Scratch repositories under the session
scratchpad with `mise x node@24.18.1 --`; evidence captures merge
stderr; every controller and implementer shell sequence is `&&`-gated;
every controller slip is logged when it happens, gate-caught or not
(T3's own standard, applied from the batch start). Never stage
docs/orchestrator-prompt.md.

## Code-health scan

Spec §6: 21 duplicated mkdtemp idioms across seven test files become
one shared helper — extract, do not duplicate; no test asserts less
than before. One sentence each in implementer.md and the orchestrating
skill, no restructuring. One new root-only knowledge entry with a
proven loading path and a ≤160-character summary.

## Pre-flight

- T1 is test-infrastructure: the helper registers cleanup at the
  mkdtemp site; the decisive live-run evidence is a before/after count
  (a full suite run adds zero new scratch directories to the temp
  root). The knowledge entry is root-only — the seed has no tests
  area; seeding a tests-area entry broke fresh-install gates once
  (quality.pin-copies-byte-exact precedent).
- T2 changes implementer.md: expected evals-rerun red until T4,
  disclosed in concerns. T3 is skill-only: no scenario covers it, no
  additional run.
- T4 judges T2's sentence live: the dependency-add scenario's collision
  disclosure lands in concerns, so its report status must read
  DONE_WITH_CONCERNS under the new tie.
- No contract change: no refresh sweep expected.

### Task 1: the scratch-dir helper and its rule (HR-035)

Files: `tests/*.test.mjs` (seven files, 21 call sites), one new helper
under `tests/`, `knowledge/houserules.json` + rendered rules. TDD per
the timing key; live-run orphan count before/after.

### Task 2: the status tie (HR-036)

Files: `template/.claude/agents/implementer.md` + root sync. Data-only.

### Task 3: the widened ledger bullet (HR-037)

Files: orchestrating skill (template + root). Data-only.

### Task 4: fifth evaluation record

Controller-driven per process.eval-fixture-procedure; `docs(evals)`
commit keyed by both template blob ids at the branch head.

## Dispatch data

- T1 Knowledge: houserules.node-under-mise,
  houserules.vitest-restore-mocks-scope, process.tdd,
  process.deliverables-json, process.gate-shell-chains,
  knowledge-base.rules-need-a-loading-path,
  knowledge-base.summary-is-the-rule,
  knowledge-base.state-only-the-source, quality.principles.
- T2/T3 Knowledge: houserules.template-is-the-source,
  process.evals-rerun, process.deliverables-json, process.tdd,
  writing-style.principles, quality.principles,
  houserules.node-under-mise, knowledge-base.state-only-the-source.
- Models: implementer sonnet, task review opus, branch review fable;
  one tier up after two endpoint drops. Strictly sequential.
