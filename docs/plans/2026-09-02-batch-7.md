# Plan: batch 7 — report contract, template tightenings, eval run

Spec: `docs/specs/2026-09-02-batch-7-report-contract-and-templates.md`
(approved 2026-09-02 with the required-fields amendment). Branch `batch-7`;
base `a5a7974`. Workspace `.superpowers/sdd/2026-09-02-batch-7/` (ledger
`progress.md`, briefs, reports, reviews, audits). Every task: one
Conventional Commit (the hook gates both harness trailers and runs
commitlint; header and body lines at 100; never write a literal
trailer-shaped line or the trailer names in prose — the audit greps
anchored now, the hook still gates lines), TDD per `process.tdd` (name the
cycle's form; disclosed mutation or before/after captures where no natural
RED exists), report at `REPORT_FILE`, task review (opus), branch review
(fable) with `--workspace` after T7.

Commands under mise (`mise run test|lint`, `mise exec -- pnpm exec vitest
run tests/<file>.test.mjs --coverage.enabled=false`, `mise exec -- node
bin/houserules.mjs update --dir .`). Never `npm`/`npx`. Scratch
repositories under the session scratchpad with `mise x node@24.18.1 --`;
live-run entries per `houserules.live-run-recipe`. Never stage
docs/orchestrator-prompt.md.

## Code-health scan

Spec §6: the plain-object guard exists once inline in `mergeSettings` — T1
extracts one shared helper, no second inline copy; the audit summary is
built in one place — T2 extends it there; the deliverables schema reuses
`$defs.run` and closed enums — no free-form objects; templates gain one to
three sentences each, no restructuring.

## Pre-flight

- Schema transition: T3 and T4 make `live_run` and `mode` REQUIRED. The T1
  and T2 reports are written and validated under the old schema; after T4
  closes, T1 and T2 get a report-only refresh round (live_run populated
  from their scratch entries, `mode` on every cycle), re-validated, with
  scoped re-reviews — so the whole workspace is schema-consistent and the
  new live_run report-field warn row reads pass at the branch audit. T5
  and later reports carry the fields natively.
- Expected-red co-change: T3 through T6 each sync a root agent template,
  so their audits carry one fail row (process.evals-rerun co-change) until
  T7 appends the record. The spec schedules this; implementers note it in
  `deviations`, reviewers treat it as the spec's accepted state, not a
  finding.
- T7 runs after all template edits: every scenario in `.claude/evals/` in
  a detached scratch worktree at the branch head; implementer scenarios on
  sonnet, seeded-violations through task-reviewer on opus; artifacts carry
  the `eval-` prefix; the record append is its own `docs(evals)` commit.
- Every review and re-review dispatch carries the identical
  `Knowledge:`/`Backlog:`/`--ids` block as round 0 (the batch 6 lesson;
  T6 writes it into the templates).
- The commit-msg hook is live: every commit in this batch passes the
  trailer gate and commitlint.

### Task 1: the shared stamp guard (HR-022)

Files: `bin/houserules.mjs` (helper + stamp read), `tests/init.test.mjs`.
RED first: `null` in `.houserules.json` (raw stack today); HR-010 stamp
tests and batch 6 settings tests stay green through the extraction. Live
run per spec T1.

### Task 2: empty-range audit stamping (HR-024)

Files: `template/tools/kb.mjs`, `tests/kb.test.mjs`, root sync.
RED first: base==head audit asserts `empty_range: true` and one prefixed
evidence string; populated ranges unchanged.

### Task 3: required live_run (HR-016)

Files: `.claude/schemas/deliverables.json` + template copy,
`knowledge/houserules.json` + `template/knowledge/houserules.json`? — no:
`houserules.live-run-recipe` lives in the root-only houserules topic; the
`report-field` check goes there (root only). `template/.claude/agents/
implementer.md` + root via update, validate fixtures in tests.
RED first per spec T3 (report without live_run fails validation).

### Task 4: required tdd mode (HR-017)

Files: both schema copies, `implementer.md` (template + root), fixtures.
RED first per spec T4 (cycle without mode fails; unknown mode rejected).

### Task 5: verdict.text (HR-021)

Files: both schema copies, `task-reviewer.md` (template + root), fixtures.
RED first per spec T5.

### Task 6: the re-review dispatch block (HR-023)

Files: orchestrating skill (template + root), `task-reviewer.md`
(template + root). Docs-only: gates and render drift are the evidence.

### Task 7: the second evaluation record

Controller-driven per the orchestrating skill's Template evaluation
section; `docs(evals)` commit appends the run set keyed by both template
blob ids at HEAD.

## Dispatch data

- T1 Knowledge: houserules.template-is-the-source, houserules.live-run-recipe,
  houserules.node-under-mise, process.tdd, process.deliverables-json,
  security-hygiene.verify-current-docs, writing-style.doc-comments,
  process.conventional-commits, quality.principles.
- T2 Knowledge: T1's list plus houserules.payload-runs-on-builtins and
  houserules.glob-union-matcher, minus process.deliverables-json.
- T3/T4 Knowledge: T1's list plus process.evals-rerun and
  knowledge-base.rules-need-a-loading-path (T3's check edit).
- T5/T6 Knowledge: houserules.template-is-the-source, process.evals-rerun,
  process.deliverables-json, process.tdd, writing-style.principles,
  process.conventional-commits, quality.principles,
  houserules.node-under-mise.
- Models: implementer sonnet, task review opus, branch review fable; one
  tier up after two endpoint drops. Strictly sequential; the T1/T2 report
  refresh rounds run after T4 closes, before the T5 dispatch.
