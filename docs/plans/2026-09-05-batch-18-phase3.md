# Plan: batch 18 — derived from the approved phase-3 spec

Written 2026-09-05 AFTER the spec's approval, under the strict
spec-then-plan order (process.brainstorm-first; the prior plan was
voided under design.md §5.32). Spec:
docs/specs/2026-09-05-batch-18-phase3.md (approved as amended);
parent docs/specs/2026-09-04-batch-15-tier2-spec.md §5.3. Items:
HR-062 (under HR-047), HR-060, HR-061, HR-051.

Branch `batch-18`; base: main tip at branch time (fetch first).
Workspace `.superpowers/sdd/2026-09-05-batch-18/` (ledger
progress.md). Process per the standing set: strictly sequential
sonnet implementers, opus task reviews, fable branch review; every
audit WITH --report; closure claims carry enumerations; the claims
checker is each report's closing act (the cargo one from T1's
completion onward); evidence at durable paths; aggregation with
explicit path staging and tree identity; the standing
rebase-onto-main rule at the finish.

## Task derivation from the spec

Each task names the spec sections it implements. One ordering note
the spec leaves implicit: the embed mechanism reads `template/` at
COMPILE time, so T3's binary embeds whatever the tree holds; the
§2 end state (the post-rewrite payload) is reached when T5
rewrites `template/` and the binary rebuilds — no separate
re-embed step exists.

### T1 — the process layer (spec §7; HR-061, HR-060, HR-051b)

The report-claims checker as a cargo bin: the five checks, the
honest Limits section, the unresolvable-sha error and the
aggregation-window limit carried over; cargo tests; the `.mjs` and
`tests/check-report-claims.test.mjs` deleted; the
process.claims-match-artifacts bullet's invocation updates to the
cargo form. Template clauses: implementer.md gains
audits-of-record-WITH---report (fix-diff audits included) and the
checker closing act; the verbatim-output invariant lifts onto its
own line (HR-051b, wording unchanged); task-reviewer.md gains
verify-by-running (a finding stating a measurement, a
reproduction, or a proposed fix's behavior is run before filing).
The spec's no-CI-job resolution for the checker is recorded in the
knowledge bullet. ONE evals rerun over every scenario closes the
task (process.evals-rerun; record.json keyed at the new blobs).

### T2 — check-commit and the hook (spec §6)

`houserules check-commit` reusing the audit's commits rules; the
CLI shape settles against how the hook and CI invoke commitlint
today (a message file from the hook's stdin/arg; a range in CI) —
the task documents both call sites before coding.
template/.githooks/commit-msg swaps its commitlint probe for a
`houserules` probe with the same degrade-to-trailer-gate shape;
the root copy syncs through the still-JS update. The repo's own
CI keeps commitlint until T6.

### T3 — init and files on the embedded payload (spec §§1-2)

rust-embed at the owner-ruled choice: exact-pinned via `cargo add
rust-embed@=<version>`, the deep dependency vet including the
self-hosted-repository check. `files` prints the ownership split;
`init` seeds a scratch repo byte-identical to the JS init EXCEPT
where the sanctioned rewrite surface will differ (until T5 the
payload is unchanged, so parity is byte-exact NOW; the diff-shape
gate takes over at T5); the stamp writes {version, idPrefix};
id-prefix rewriting in the seeded schemas; the live-run recipe in
scratch git repos (houserules.live-run-recipe — its npm-era
bullets update at T6).

### T4 — update with KIT_OWNED sync and deletion (spec §1)

Byte-parity KIT_OWNED sync; the deletion capability (kb.sh,
backlog.sh leave installs at the next update); the drift line
(`kit <old> -> <new>`, absence renders `none`, an invalid stamp is
a named error per the crash-path convention); the settings merge
as a parity port of the JS's targeted semantics (spec §7b: no
generic merge crate — it would change behavior). Live proof: init
a scratch repo with the OLD JS kit, update with the binary, assert
the deletions and that only kit-owned files changed.

### T5 — the reference rewrite and the golden re-baseline (spec §3)

The scripted enumeration of every shipped reference to
tools/kb.sh|backlog.sh (the closure artifact, rerunnable); the
rewrite across template/ and the Rust render templates
(STANDING_COMMAND, the rendered `Detail:` line, the skill's
retrieval protocol); the render goldens split out of the frozen
corpus into tests/goldens/render/ regenerated by a cargo dev
command; make-corpus drops its render slices; THE DIFF-SHAPE GATE:
a retained script proving old-corpus-vs-new-golden diffs contain
only lines matching the explicit command-mapping table; the skill
and rules budgets re-checked against the rewritten output.

### T6 — the repository self-flip and the invariant amendment
(spec §§4-5, §1)

Dogfood the binary's update over this repository (the root shell
tools die); CLAUDE.md, the mise tasks, the CI checks job, the
SessionStart hook settings, and the orchestrating skill's ritual
lines flip to `houserules` commands, with the binary fronted by a
mise task; commitlint leaves the repo CI for check-commit; the
parent spec's §2 invariant amendment + design.md §3 ownership
prose + houserules.payload-runs-on-builtins rewritten to the
ruled binary+POSIX contract; houserules.live-run-recipe updates;
HR-051a's validate extension (fix_rounds[].tests[].output scanned
for a nonzero-skipped audit summary) lands with its parent-spec §6
record and its own fixtures.

## Code-health scan (the files this batch touches)

- bin/houserules.mjs: the last unported surface; port faithfully,
  no JS refactor — it remains the frozen-corpus reference.
- .githooks/commit-msg (template + root): probe-block swap only,
  the degrade shape kept.
- .claude/agents/implementer.md, task-reviewer.md: the T1 clauses;
  template edits pay the one shared evals rerun.
- tools/check-report-claims.mjs (+ vitest test): deleted at T1;
  its cargo successor carries the tests.
- The reference-bearing shipped files (CLAUDE.md, three skills,
  three agents, knowledge seed bodies, the generated artifacts):
  enumerated by T5's script before any edit — the enumeration
  bounds the rewrite claim.
- Library posture per spec §7b: rust-embed is the one new domain;
  every task folds in refactors for custom code it touches,
  escalating candidates to the controller for owner ruling.

## Verification (controller, after T6 — spec §10's acceptance)

The scratch adopter flow end to end: `houserules init` a fresh
repo and run its checks through the binary; `houserules update`
over an old JS-kit install, the shell tools leave, only kit-owned
files change; this repository's own gates run through the binary;
check-commit gates a real commit range; the full gate set (cargo,
both vitest configs, the flipped lint path); branch review (fable)
with --workspace; the finish per finishing-a-feature with the
rebase rule, aggregation with tree identity, PR, three-OS CI,
ff-only merge.

## Dispatch data

- Knowledge ids per dispatch from `tools/kb.sh for` (T1-T5;
  `houserules for` once T6 flips) plus, as the task warrants:
  process.claims-match-artifacts,
  process.closure-claims-carry-enumeration,
  process.review-findings-are-claims-too,
  process.evidence-outlives-the-session,
  quality.well-maintained-libraries, process.evals-rerun (T1),
  houserules.live-run-recipe (T3/T4),
  houserules.crash-paths-are-named and glob-union-matcher (rust
  area), houserules.pinned-shas-live-on-mains-ancestry (fixtures),
  houserules.vitest-coverage-floor-tracks-the-rust-port (where
  vitest cases move). Five to ten ids each.
- Models: implementer sonnet, task review opus, branch review
  fable; one tier up after two endpoint drops. Strictly
  sequential; no task starts before its predecessor's approval.
