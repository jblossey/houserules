# Plan: batch 9 — predicate, pin, protocol wording

Spec: `docs/specs/2026-09-02-batch-9-predicate-pin-and-protocol.md`
(approved 2026-09-02, as written). Branch `batch-9`; base `860748a`.
Workspace `.superpowers/sdd/2026-09-02-batch-9/` (ledger `progress.md`,
briefs, reports, reviews, audits). Every task: one Conventional Commit
(the hook gates both harness trailers and runs commitlint; header/body
lines at 100; lowercase subject start), TDD per `process.tdd` with
`mode` per the timing key, report per the full contract, task review
(opus), branch review (fable) with `--workspace` after T5. The audit
`--ids` on every round are the dispatch's Knowledge ids, verbatim.

Commands under mise; pnpm only. Scratch repositories under the session
scratchpad with `mise x node@24.18.1 --`; evidence captures merge stderr;
every controller and implementer shell sequence is `&&`-gated; guard
catches get a ledger line when they happen. Never stage
docs/orchestrator-prompt.md.

## Code-health scan

Spec §6: two idPrefix validators become one shared predicate (extract,
no duplicate; one message); the scenario pin sits beside the two
existing pins, byte-exact, no normalizer; one added sentence per
template file, no restructuring; the retrieval protocol gains one
clause.

## Pre-flight

- T1 touches the guard T4-of-batch-8 moved: the shared predicate lands
  inside the pre-write validation block; the untouched-target invariant
  must survive (the existing snapshot tests pin it).
- T2's pin is byte-exact with NO designed difference — simpler than the
  schema pin; `record.json` exclusion stated in the comment.
- T3/T4 carry the expected evals-rerun red until T5, disclosed in
  concerns; T4's kb.mjs retrieval-protocol clause is invisible to the
  co-change check but the rule body demands the run — T5's record note
  names it.
- T5 judges the batch's own protocol sentences in action: the eval
  dispatches themselves use --ids = Knowledge ids.
- No contract change: no refresh sweep expected.

### Task 1: the shared idPrefix predicate (HR-031)

Files: `bin/houserules.mjs`, `tests/init.test.mjs`. RED first (natural):
"wi" and "TOOLONGPREFIX" stamps pass today. Live run per spec T1.

### Task 2: the scenario-copy pin (HR-032)

Files: `tests/dogfood.test.mjs`. Disclosed mutation, mode mutation.

### Task 3: the timing-key example (HR-033)

Files: `template/.claude/agents/implementer.md` + root sync. Data-only.

### Task 4: the --ids protocol wording (HR-034)

Files: orchestrating skill (template + root), `template/tools/kb.mjs`
retrieval protocol + root sync. Data-only.

### Task 5: fourth evaluation record

Controller-driven per process.eval-fixture-procedure; `docs(evals)`
commit keyed by both template blob ids at the branch head.

## Dispatch data

- T1 Knowledge: houserules.template-is-the-source, houserules.live-run-recipe,
  houserules.node-under-mise, process.tdd, process.deliverables-json,
  security-hygiene.verify-current-docs, writing-style.doc-comments,
  quality.principles, quality.no-compat-softening.
- T2 Knowledge: houserules.template-is-the-source, quality.pin-copies-byte-exact,
  process.tdd, process.deliverables-json, quality.principles,
  process.gate-shell-chains, writing-style.code-comments,
  houserules.node-under-mise.
- T3/T4 Knowledge: houserules.template-is-the-source, process.evals-rerun,
  process.deliverables-json, process.tdd, writing-style.principles,
  quality.principles, houserules.node-under-mise,
  knowledge-base.state-only-the-source.
- Models: implementer sonnet, task review opus, branch review fable; one
  tier up after two endpoint drops. Strictly sequential.
