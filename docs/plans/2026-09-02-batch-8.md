# Plan: batch 8 — audit reach, stamp strictness, eval polish

Spec: `docs/specs/2026-09-02-batch-8-audit-reach-and-stamp-strictness.md`
(approved 2026-09-02, as written). Branch `batch-8`; base `5586b05`.
Workspace `.superpowers/sdd/2026-09-02-batch-8/` (ledger `progress.md`,
briefs, reports, reviews, audits). Every task: one Conventional Commit
(the hook gates both harness trailers and runs commitlint; header and
body lines at 100; never a literal trailer-shaped line — the audit's
anchored check no longer trips on prose, the hook still gates lines), TDD
per `process.tdd` with `mode` named per the timing key (ruling 18),
report per the full contract (`live_run` populated, `mode` on every
cycle), task review (opus), branch review (fable) with `--workspace`
after T7.

Commands under mise; pnpm only, never `npm`/`npx`. Scratch repositories
under the session scratchpad with `mise x node@24.18.1 --`;
evidence-producing shell sequences are `&&`-chained
(`process.gate-shell-chains`). Never stage docs/orchestrator-prompt.md.

## Code-health scan

Spec §6: the package condition at `template/tools/kb.mjs:709` is one
boolean — T2 extends it, no fork; `install`'s write-then-validate order
inverts with one validation path (T3/T4, no second stamp parser); the
schema-copy test reuses the dogfood style (T1); the scenario gains one
line (T5); the template edit moves words without changing them (T6).

## Pre-flight

- T2 changes audit-package composition mid-batch: from T2 on, audits over
  `bin/**` changes (T3, T4) carry the live_run report-field row without
  `--ids` — the reviewer verifies the widened package as T2's own
  consequence; earlier audits (T1) are unaffected (tests area carries no
  checked entry).
- T2 updates `knowledge-base.rules-need-a-loading-path`'s body in the
  same commit (the interim "until HR-026" clause), root-only entry, with
  render.
- T4 inverts install's order: the T3 idPrefix validation must land first
  so T4 validates the complete stamp contract before writing.
- T5 and T6 each sync a template/eval file: their audits carry the
  expected evals-rerun fail row until T7, disclosed in concerns per the
  rule body's batch pattern.
- T6 is structure-only: the diff-stat proof (words moved, none changed)
  is part of its evidence; dogfood parity pins both copies.
- T7 follows `process.eval-fixture-procedure` for the seeded fixture and
  judges T5's new criterion for the first time.
- No contract change this batch: no refresh sweep is expected
  (`process.contract-refresh-sweep` stays dormant).

### Task 1: the schema-copy sync test (HR-025)

Files: `tests/` (beside dogfood parity concerns). Disclosed mutation
(one-field drift, RED, restore byte-identical, GREEN); `mode: mutation`.

### Task 2: every check gets an audit loading path (HR-026)

Files: `template/tools/kb.mjs` (+ root sync), `tests/kb.test.mjs`,
`knowledge/knowledge-base.json` (body update) + render outputs.
RED first (natural): a checked procedure in a matched area joins the
package; an unchecked procedure still does not.

### Task 3: idPrefix is a non-empty string (HR-027)

Files: `bin/houserules.mjs`, `tests/init.test.mjs`. RED first (natural):
a numeric idPrefix flows into id generation today. Live run per spec T3.

### Task 4: the stamp is read before writing (HR-028)

Files: `bin/houserules.mjs`, `tests/init.test.mjs`. RED first (natural):
today a corrupt stamp exits 2 with the target already updated. Live run
per spec T4 with the tree snapshot.

### Task 5: the collision as a judged criterion (HR-029)

Files: `.claude/evals/dependency-add.json` (root + template if seeded —
verify; the evals directory is SEED_ONCE at root). Data-only; gates.

### Task 6: one report field per line (HR-030)

Files: `template/.claude/agents/implementer.md` + root sync. Data-only;
gates + the diff-stat word-preservation proof.

### Task 7: third evaluation record

Controller-driven per the orchestrating skill and
`process.eval-fixture-procedure`; `docs(evals)` commit keyed by both
template blob ids at the branch head.

## Dispatch data

- T1 Knowledge: houserules.template-is-the-source, process.tdd,
  process.deliverables-json, quality.principles, quality.no-compat-softening,
  process.gate-shell-chains, writing-style.code-comments,
  process.conventional-commits.
- T2 Knowledge: T1's list plus houserules.payload-runs-on-builtins,
  houserules.glob-union-matcher, knowledge-base.rules-need-a-loading-path,
  knowledge-base.state-only-the-source, houserules.live-run-recipe,
  houserules.node-under-mise.
- T3/T4 Knowledge: houserules.template-is-the-source,
  houserules.live-run-recipe, houserules.node-under-mise, process.tdd,
  process.deliverables-json, security-hygiene.verify-current-docs,
  writing-style.doc-comments, process.conventional-commits,
  quality.principles, quality.no-compat-softening.
- T5/T6 Knowledge: houserules.template-is-the-source, process.evals-rerun,
  process.deliverables-json, process.tdd, writing-style.principles,
  process.conventional-commits, quality.principles,
  houserules.node-under-mise.
- Models: implementer sonnet, task review opus, branch review fable; one
  tier up after two endpoint drops. Strictly sequential; every review
  round carries the identical `Knowledge:`/`Backlog:`/`--ids` block.
