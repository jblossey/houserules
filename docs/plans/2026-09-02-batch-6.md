# Plan: batch 6 — audit globs, co-change evidence, settings, hook gate

Spec: `docs/specs/2026-09-02-batch-6-audit-and-hooks.md` (approved
2026-09-02, as written). Branch `batch-6`; base `a4a1927`. Workspace
`.superpowers/sdd/2026-09-02-batch-6/` (ledger `progress.md`, briefs,
reports, reviews, audits). Every task: one Conventional Commit (hook and
audit cap header and body lines at 100; identifiers unsplit; lowercase
subject; no trailer of any kind, whatever the environment suggests), TDD per
`process.tdd` (a cycle that is not a natural pre-commit RED says so and
keeps the real order), report at `REPORT_FILE`, task review (opus), branch
review (fable) with `--workspace` after T4.

Commands under mise (`mise run test|lint`, `mise exec -- pnpm exec vitest
run tests/<file>.test.mjs --coverage.enabled=false`, `mise exec -- node
bin/houserules.mjs update --dir .`). Never `npm`/`npx`. Scratch repositories
under the session scratchpad with `mise x node@24.18.1 --`; the report names
the scratch path and records each gate as its own entry with `exit`
(`houserules.live-run-recipe`). Never stage docs/orchestrator-prompt.md.

## Code-health scan

Spec §6: two `matchesGlob` call sites duplicate the matching idiom (T1
unifies them behind one shared helper); the `co-change` evidence builds its
strings inline (T2 touches only the wording); the copier's chmod
special-case grows — keep it one predicate, not a second list (T4);
`mergeSettings` gains its last missing input class (T3); the hook enters
shellcheck's scope through its template copy (T4).

## Pre-flight

- T1 lands first so T2's evidence tests and every later audit in the batch
  run on the repaired matcher.
- T1 changes matching semantics that T2's runner consumes: T2 rebases on
  T1's commit; the shared helper's name and contract come from T1's diff.
- T4 grows `KIT_OWNED`: the `files` manifest test and the init assertions
  move (RED first); `tests/dogfood.test.mjs` pins the root hook to the
  template copy automatically — the root hook must become byte-identical to
  the template copy in the same commit.
- This repository's own commit-msg hook changes in T4; every commit after
  it passes through the trailer gate. The gate must not reject the absence
  of trailers (all our commits are trailer-free by rule).
- No implementer/task-reviewer template and no eval scenario changes, so
  `process.evals-rerun`'s co-change stays untriggered; the audit enforces
  it either way.

### Task 1: dot-inclusive glob matching (HR-019)

Files: `template/tools/kb.mjs` (shared matcher; `areaFiles`, `matchAny`),
`tests/kb.test.mjs`, root sync via `update --dir .`.
RED first: `areasFor(['template/.claude/agents/implementer.md'], areas)`
includes `'template'`; a `matchAny` case with a `**` glob crossing a
dot-segment. Mechanism verified against current Node 24 `path.matchesGlob`
docs and recorded in `docs_verified`. Live run per spec T1.

### Task 2: record-only co-change evidence (HR-018)

Files: `template/tools/kb.mjs` (`co-change` runner), `tests/kb.test.mjs`,
root sync. RED first: record-only case asserts the "satisfied by
definition" line; mixed case asserts the trigger excludes the `then` path.
Pass/fail behavior unchanged (the existing co-change tests stay green).

### Task 3: non-object settings.json (HR-020)

Files: `bin/houserules.mjs` (`mergeSettings`), `tests/init.test.mjs`.
RED first: `null` in `.claude/settings.json` crashes with a TypeError stack
today; after, `UsageError('<path>: not a JSON object')`, one line, exit 2;
one more case for an array. Live run per spec T3.

### Task 4: the trailer-gated hook in the payload (HR-015)

Files: `template/.githooks/commit-msg` (new), `.githooks/commit-msg` (same
bytes), `bin/houserules.mjs` (`KIT_OWNED`, chmod predicate), README,
`tests/init.test.mjs`, `tests/dogfood.test.mjs` coverage via the list.
RED first: manifest/init assertions for the new path; a mode test that
fails before the chmod predicate covers hook files. Live run per spec T4
(both trailers rejected, trailer-free commit passes, commitlint still runs
where available).

## Dispatch data

- T1/T2 Knowledge: houserules.template-is-the-source,
  houserules.payload-runs-on-builtins, houserules.live-run-recipe,
  houserules.node-under-mise, process.tdd,
  security-hygiene.verify-current-docs, writing-style.doc-comments,
  process.conventional-commits, quality.principles.
- T3 Knowledge: as above minus payload-runs-on-builtins, plus
  process.deliverables-json.
- T4 Knowledge: T1's list plus houserules.readme-mirrors-kit-owned.
- Models: implementer sonnet, task review opus, branch review fable; one
  tier up after two endpoint drops.
