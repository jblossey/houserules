# Plan: batch 5 — the migration skill, and the settings read

Spec: `docs/specs/2026-09-02-batch-5-migration-skill.md` (approved
2026-09-02). Branch `batch-5`; base `2a535f6`. Workspace
`.superpowers/sdd/2026-09-02-batch-5/` (ledger `progress.md`, briefs,
reports, reviews, audits). Every task: one Conventional Commit (hook and
audit cap header and body lines at 100; identifiers unsplit; lowercase
subject; no trailer of any kind, whatever the environment suggests), TDD per
`process.tdd` (a cycle that is not a natural pre-commit RED says so and
keeps the real order), report at `REPORT_FILE`, task review (opus), branch
review (fable) with `--workspace` after T2.

Commands under mise (`mise run test|lint`, `mise exec -- pnpm exec vitest
run tests/<file>.test.mjs --coverage.enabled=false`, `mise exec -- node
bin/houserules.mjs update --dir .`). Never `npm`/`npx`. Scratch repositories
under the session scratchpad with `mise x node@24.18.1 --`; the report names
the scratch path and records each gate as its own entry with `exit`
(`houserules.live-run-recipe`). Never stage docs/orchestrator-prompt.md.

## Code-health scan

Spec §6: `mergeSettings` is the last hand parse of project data (T2);
the ownership lists absorb one skill path (T1); the new skill follows the
existing one-file shape. No other smell in the touched files.

## Pre-flight

- T1 grows `KIT_OWNED`: the `files` manifest test and the init assertions
  move (RED first); `tests/dogfood.test.mjs` covers the new root copy
  automatically once the list grows.
- T1 changes no implementer/task-reviewer template and no eval scenario, so
  `process.evals-rerun`'s co-change stays untriggered; the audit enforces it
  either way.
- T2's message-wording change is pinned by the existing settings test
  (exit 2 + the file name); only the message expectation moves.
- The skill's prose has no natural RED; the live mini-migration in the
  scratch repository is the evidence, each step its own report entry.

### Task 1: the migrating-knowledge skill (HR-013)

**Files:**
- Create: `template/.claude/skills/migrating-knowledge/SKILL.md`
- Modify: `bin/houserules.mjs` (`KIT_OWNED` gains
  `.claude/skills/migrating-knowledge/SKILL.md`, in list order),
  `README.md` ("Apply to an existing project" gains one pointer sentence),
  `tests/init.test.mjs` (manifest and init assertions, RED first)
- Synced by `update --dir .`: `.claude/skills/migrating-knowledge/SKILL.md`

**Skill content (spec §4 T1 lists the eight sections; write them in the
kit's voice):** frontmatter `name: migrating-knowledge` and a `description`
that triggers on migrating existing knowledge / adopting houserules in an
existing codebase; sections When / Inventory / Classify / Write entries /
Areas / Migrate topic by topic / Gates / A worked example. Ground every
instruction in the schema and the `knowledge-base.*` rules; the worked
example shows one CLAUDE.md paragraph becoming one complete entry
(before/after, with summary length and a `verify` path). Keep it one
screen per section; short active sentences.

**Steps:**
- [ ] Step 1, RED: the ownership-list assertions expect the new path →
  focused tests/init.test.mjs fails before the list and file exist.
- [ ] Step 2: write the skill; add the `KIT_OWNED` entry; `update --dir .`;
  README sentence; focused GREEN; full `mise run test`; `mise run lint`.
- [ ] Step 3, live run (scratch, cd-carrying entries with `exit`): `init`
  seeds the skill (`ls` it); then the mini-migration follows the skill's own
  steps — write a small CLAUDE.md with two embedded rules into the scratch,
  create a topic file with one migrated entry (summary ≤ 160, source,
  verify), extend the area enum and globs for it, park one leftover in
  `backlog/parked.json`, delete the migrated paragraph, `tools/kb.sh render`
  and both checks green.
- [ ] Step 4: commit `feat(template): a skill that migrates existing
  knowledge into the kit`; report (tdd: the list-change cycle natural; the
  prose disclosed as no-natural-RED); validate; audit
  `--base <BASE> --head HEAD`; self-review.

### Task 2: settings.json through readJson (HR-014)

**Files:**
- Modify: `bin/houserules.mjs` (`mergeSettings`), `tests/init.test.mjs`

**Behavior:** `mergeSettings` reads with `readJson`; invalid JSON →
`UsageError('<path>: invalid JSON (...)')`, one stderr line, exit 2; an
unreadable file propagates as a plain Error (the recorded semantics
decision; the batch 4 `EISDIR` idiom pins it). The old
`not readable JSON` wording goes.

**Steps:**
- [ ] Step 1, RED: the settings test's message expectation moves to the
  `readJson` wording and asserts one stderr line, no stack frame; a
  companion pins the plain-Error path (a directory as
  `.claude/settings.json`). Focused RED.
- [ ] Step 2: implement; focused GREEN; full suite (coverage gate; the
  rethrow branch keeps its covers); lint.
- [ ] Step 3, live run (scratch): `init`; corrupt `.claude/settings.json`;
  `init` again → one line naming the file, `EXIT: 2`; restore; ok.
- [ ] Step 4: commit `fix(cli): read settings.json through readjson`;
  report; validate; audit; self-review.
