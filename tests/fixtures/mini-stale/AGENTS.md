# Mini-stale fixture

Synthetic project root for the stale-render corpus slice (HR-054
mini-stale fixture). The over-budget file this scenario exercises is
CLAUDE.md, kept present but never required, on purpose.
