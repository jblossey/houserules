{
  "$schema": "./schema.json",
  "topic": "process",
  "title": "How work runs: batches, dispatch, reviews, rulings",
  "entries": [
    {
      "id": "process.ask-when-missing",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Ask the user when information is missing. Do not assume.",
      "body": [],
      "tags": ["users"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.backlog-drives-work",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "The backlog drives all work. Select backlog items before you start; every requirement traces to an item.",
      "body": [
        "The backlog lives in `backlog/` (JSON). Read it with `tools/backlog.sh list --open`, `get <id>`, `batch <n>`; tick items with `set <id> status=done batch=<n>`."
      ],
      "tags": ["backlog"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "verify": ["backlog/schema.json"]
    },
    {
      "id": "process.brief-carries-the-spec",
      "kind": "rule",
      "area": "process",
      "standing": false,
      "summary": "A task brief carries every test, gate, and file its spec task names; an implementer who cannot satisfy one flags it instead of dropping it.",
      "body": [
        "The controller writes briefs from the spec and plan; a silent omission surfaces only in review, one round late (batch 3, task 3).",
        "When a brief and the spec disagree, the spec wins and the disagreement is a ruling to record."
      ],
      "tags": [
        "dispatch",
        "briefs"
      ],
      "source": {
        "date": "2026-09-01",
        "by": "review",
        "ref": "batch 3 branch review, uncovered_findings; task 3 review, finding 2"
      }
    },
    {
      "id": "process.brainstorm-first",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Start each batch with a brainstorming session or a written spec; get the user's approval before implementing.",
      "body": [
        "Use the superpowers brainstorming and spec skills when the plugin is installed; otherwise write the spec by hand. Specs live in `docs/specs/`."
      ],
      "tags": ["batch", "design"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.conventional-commits",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Commits use Conventional Commits (feat, fix, chore, test, ci, docs, refactor). Header at most 100 characters, body lines at most 100.",
      "body": [
        "Start the subject in lowercase; a subject that starts with an uppercase token fails common commitlint configurations."
      ],
      "tags": ["git"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "check": {
        "type": "commits",
        "level": "fail",
        "subject": "^(?=.{1,100}$)(feat|fix|chore|test|ci|docs|refactor|perf|build|style|revert)(\\([^)]+\\))?!?: .+",
        "body_line_max": 100
      }
    },
    {
      "id": "process.ff-only-merges",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Merge fast-forward only, from the CLI, after aggregating the branch into clean logical commits. No merge commits, no GitHub squash merges.",
      "body": [
        "Aggregate with `git reset --soft $(git merge-base main HEAD)` and recommit in one to five Conventional Commits when interactive rebase is not available.",
        "The project skill `finishing-a-feature` is the procedure."
      ],
      "tags": ["git"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "verify": [".claude/skills/finishing-a-feature/SKILL.md"]
    },
    {
      "id": "process.knowledge-first",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Before you change a file, read its knowledge: the ids in your task and `tools/kb.sh for <path>`. Cite the ids you relied on in your report.",
      "body": [],
      "tags": ["knowledge"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "check": { "type": "report-field", "level": "warn", "if": "**", "field": "knowledge_used" }
    },
    {
      "id": "process.rulings-to-file",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Every ruling goes to its home file in the same turn — a deferral’s backlog item included. A ledger and the chat are not home files; neither survives compaction.",
      "body": [
        "Home files: scope or product rulings go to a backlog entry; process rulings to a knowledge entry (`standing: true` when non-negotiable); design rulings to the batch spec.",
        "A ledger note may accompany a ruling; it never substitutes for the tracked write.",
        "A dispatch deviation from the spec's process line (model, order, gate) is a ruling: amend that spec line in the same turn. The batch report at the tick repeats it; it does not replace it."
      ],
      "tags": ["rulings", "compaction"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.sequential-agents",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Run all agent work strictly sequentially. Never dispatch two implementers or two reviewers at the same time.",
      "body": [
        "Parallel dispatches conflict in the working tree and hide which run produced a change."
      ],
      "tags": ["dispatch"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.tdd",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Test-driven development for every executable change: the failing test first, or a disclosed-mutation proof for already-correct behavior; verbatim RED and GREEN.",
      "body": [
        "Data-only and docs-only changes carry no RED/GREEN. The report's `tests` names the gates that ran instead (`kb check`, `backlog check`, `validate`, render drift, lint); the reviewer judges their sufficiency.",
        "A data change with a testable consumer — a check pattern with a regression test, a rendered artifact a test pins — is a code change for this rule: the consumer test comes first.",
        "A new test for behavior that is already correct proves it can fail through a deliberate, disclosed mutation, not a natural RED.",
        "Never fabricate a cycle: an empty `tdd` with named gates is honest evidence; a manufactured RED is not."
      ],
      "tags": ["testing"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.model-policy",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Every review runs on a mightier model than the implementer it reviews. Implementers use the cheapest model that fits the task.",
      "body": [
        "Model order: haiku < sonnet < opus < fable. Mightier is strict: an Opus implementer needs a Fable review, so choosing an Opus implementer is a deliberate, costed decision.",
        "Scoped re-reviews follow the same bar as first reviews: the fix diff's size never lowers the reviewer's tier.",
        "Name the model on every dispatch; an omitted model inherits the session's most expensive one."
      ],
      "tags": ["dispatch", "models"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.no-tech-debt",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Fix every review finding, Minor included. Defer a fix only for a stated reason, as a backlog item; never as a TODO in code.",
      "body": [
        "Technical debt is a no-go. A finding a reviewer filed is fixed in the same fix round, whatever its severity.",
        "The only way to defer: a backlog item that states the reason and the follow-up; the finding names the item. A ledger line or a chat note is not a deferral.",
        "No `TODO`, `FIXME`, or `HACK` marker in source files; the deterministic check fails the audit on a new one. Todos live in `backlog/`."
      ],
      "tags": ["review", "debt"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "see": ["quality.principles"],
      "check": {
        "type": "grep-absent",
        "level": "fail",
        "scope": "changed",
        "files": [
          "**/*.ts", "**/*.tsx", "**/*.mts", "**/*.mjs", "**/*.js", "**/*.jsx",
          "**/*.rs", "**/*.py", "**/*.go", "**/*.rb", "**/*.java", "**/*.kt",
          "**/*.swift", "**/*.sh", "**/*.toml", "**/*.yml", "**/*.yaml"
        ],
        "pattern": "\\b(TODO|FIXME|HACK)\\b"
      }
    },
    {
      "id": "process.code-health-scan",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Every plan carries a code-health scan of the files the batch touches: name the smells and antipatterns found, and fold targeted fixes into the tasks.",
      "body": [
        "The goal is active maintenance. The Boy Scout Rule: leave the code cleaner than you found it. Visible neglect invites more neglect, so entropy is fought at every touch, not in rescue projects.",
        "During brainstorming and plan writing, read the files the batch will change and list: code smells (duplication, long functions or files, divergent change, shotgun surgery, feature envy, primitive obsession, data clumps, speculative generality), antipatterns, and deviations from current best practice (verified against current docs, not memory).",
        "Fold the fixes in as opportunistic refactoring: a step inside the task that touches the file, or a dedicated task when the cleanup is load-bearing. Where a cleanup makes the feature change easy, do it first.",
        "Scope bound: only code the batch touches or directly depends on. An improvement outside that scope becomes a backlog item, never plan scope creep.",
        "The scan's output is part of the plan: a Code health section naming the findings, what the plan fixes, and what goes to the backlog and why. A clean scan states what was checked.",
        "This rule never justifies over-engineering: no abstraction for one caller, no configurability without a second use, no rewrite of working code the batch does not touch. Speculative generality is itself a smell."
      ],
      "tags": ["planning", "refactoring", "code-health"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "see": ["quality.principles", "process.no-tech-debt", "process.brainstorm-first"]
    },
    {
      "id": "process.deliverables-json",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Task reports, reviews, and branch reviews are JSON files that pass `tools/kb.sh validate` against `.claude/schemas/deliverables.json`.",
      "body": [
        "Every deliverable an agent writes is structured data in the batch workspace: `task-<N>-report.json`, `task-<N>-review.json`, `task-<N>-review-r<R>.json`, `branch-review.json`. The `kind` field names the definition.",
        "Write the file, run `tools/kb.sh validate <file>`, fix every error, then audit. A deliverable that does not validate is incomplete; the controller re-dispatches.",
        "The implementer's `self_audit` starts as `null` and holds the audit's `summary` and deterministic rows after the audit; the check below fails until it does. `self_audit` always covers the whole task (`BASE..HEAD` at report time); a fix round's audit over the fix diff goes into that `fix_rounds` entry's `tests`.",
        "`tools/kb.sh stats` and the branch retrospective read these files; prose is invisible to them."
      ],
      "tags": ["reports", "deliverables"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "see": ["process.knowledge-first"],
      "verify": [".claude/schemas/deliverables.json"],
      "check": { "type": "report-field", "level": "fail", "if": "**", "field": "self_audit" }
    },
    {
      "id": "process.evals-rerun",
      "kind": "rule",
      "area": "docs",
      "summary": "Re-run every `.claude/evals/` scenario when the implementer or task-reviewer template or a scenario changes; append the run to `.claude/evals/record.json`.",
      "body": [
        "The scenarios are the regression suite for the agent templates. The check is a `co-change`: a change to `.claude/agents/implementer.md`, `.claude/agents/task-reviewer.md`, or `.claude/evals/*.json` without a change to `.claude/evals/record.json` fails the audit. The entry routes through the `docs` area (`.claude/**`), so every audit that touches the templates carries it without `--ids`; the `process` area has no paths.",
        "Run every scenario in a detached scratch worktree at the branch head before the branch review; the orchestrating skill holds the procedure. Keep nothing from a worktree; workspace artifacts carry the `eval-` prefix.",
        "A run set in the record names `date`, `templates` (the git blob id of each template under test from `git rev-parse HEAD:<path>`; blob ids survive commit aggregation, commit SHAs do not), and one row per scenario: `scenario`, `agent`, `model`, `pass`, `of`, `notes`. The branch reviewer compares the latest blob ids with `HEAD`.",
        "No scenario covers `branch-reviewer` or the orchestrating skill; a change there needs no run. The retrieval protocol text in `tools/kb.mjs` shapes the generated skill the implementer loads; run the scenarios after a change there too — the check cannot see it."
      ],
      "tags": ["evals", "templates", "agents"],
      "source": { "date": "2026-08-31", "by": "user", "ref": "tag-pilot fc3241b" },
      "see": ["process.deliverables-json", "process.keep-knowledge-current"],
      "verify": [".claude/evals/record.json", ".claude/skills/orchestrating/SKILL.md"],
      "check": {
        "type": "co-change",
        "level": "fail",
        "if": [
          ".claude/agents/implementer.md",
          ".claude/agents/task-reviewer.md",
          ".claude/evals/*.json"
        ],
        "then": ".claude/evals/record.json"
      }
    },
    {
      "id": "process.live-run-before-ci",
      "kind": "rule",
      "area": "process",
      "standing": true,
      "summary": "Verify a change live (run the app, service, or tool for real; capture evidence) before any PR, merge, or deploy spend.",
      "body": [
        "CI and deploy runs cost money and time; a live run catches what unit tests cannot. Record the project's live-run recipe as a `procedure` entry."
      ],
      "tags": ["live-run", "ci"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    },
    {
      "id": "process.keep-knowledge-current",
      "kind": "rule",
      "area": "process",
      "summary": "Add knowledge future sessions need to `knowledge/`; session memory is not portable, repo files are.",
      "body": [
        "Write the entry in the same turn you learn the fact. Then run `tools/kb.sh render` and commit the generated files with it."
      ],
      "tags": ["knowledge"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" },
      "verify": ["knowledge/schema.json"],
      "see": ["process.rulings-to-file"]
    },
    {
      "id": "process.skills-for-procedures",
      "kind": "rule",
      "area": "process",
      "summary": "Create a project skill (`.claude/skills/`) for a repeatable procedure. Author it with the writing-skills skill when available.",
      "body": [],
      "tags": ["skills"],
      "source": { "date": "2026-08-30", "by": "docs", "ref": "houserules seed" }
    }
  ]
}
