{
  "name": "houserules",
  "version": "0.2.0-alpha",
  "private": true,
  "license": "MIT",
  "description": "A reusable knowledge-management and agent-workflow kit for AI-assisted repositories.",
  "type": "module",
  "bin": {
    "houserules": "./bin/houserules.mjs"
  },
  "files": [
    "bin",
    "template",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "vitest run --coverage && vitest run --coverage --config vitest.kb-coverage.config.mts"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@vitest/coverage-v8": "4.1.11",
    "vitest": "4.1.11"
  }
}
