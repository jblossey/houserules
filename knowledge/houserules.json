{
  "$schema": "./schema.json",
  "topic": "houserules",
  "title": "Working in the houserules kit repository",
  "entries": [
    {
      "id": "houserules.template-is-the-source",
      "kind": "rule",
      "area": "global",
      "standing": true,
      "summary": "Edit the kit in `template/`, then run `node bin/houserules.mjs update --dir .`; never hand-edit the root copies or the generated rules and skill.",
      "body": [
        "This repository runs its own kit. The root `tools/`, `.claude/agents/`, `.claude/skills/orchestrating`, and `.claude/skills/finishing-a-feature` are the installed copy; `template/` is the source that `init` and `update` ship to every project.",
        "`tests/dogfood.test.mjs` pins every root copy to its template source byte for byte; a hand edit fails the test and is lost on the next update.",
        "`.claude/rules/*.md` and `.claude/skills/project-knowledge/SKILL.md` are generated by `tools/kb.sh render` from `knowledge/`."
      ],
      "tags": [
        "dogfood",
        "template",
        "update"
      ],
      "source": {
        "date": "2026-08-31",
        "by": "user",
        "ref": "docs/design.md §5.5"
      },
      "verify": [
        "tests/dogfood.test.mjs",
        "bin/houserules.mjs"
      ]
    },
    {
      "id": "houserules.tag-pilot-is-read-only",
      "kind": "rule",
      "area": "global",
      "standing": true,
      "summary": "Treat `~/projects/tag-pilot` as a read-only reference: never modify it, commit there, run its agents, or run `init` or `update` against it.",
      "body": [
        "tag-pilot (branch `knowledge-management`, PR #42) is the origin of this kit and, until the adoption ruling in docs/design.md §5.4, the upstream of every kit-owned file.",
        "Before every release, diff its `tools/`, `.claude/agents/`, and `.claude/skills/` against `template/` and port the drift; the first port is `process.evals-rerun` with its `co-change` check (HR-003)."
      ],
      "tags": [
        "tag-pilot",
        "upstream",
        "release"
      ],
      "source": {
        "date": "2026-08-31",
        "by": "user",
        "ref": "orchestrator brief of 2026-08-31; docs/design.md §5.4"
      }
    },
    {
      "id": "houserules.live-run-recipe",
      "kind": "procedure",
      "area": "cli",
      "summary": "Live-run every CLI change in a fresh scratch `git init` repository: `init --dir <scratch>`, then `tools/kb.sh check && tools/backlog.sh check` there.",
      "body": [
        "Scratch repositories live under the session scratchpad, never inside this repository and never in tag-pilot.",
        "`npm exec --yes --package=git+file://<this clone> -- houserules files` exercises the npm `.bin` path that `npx github:` and `npx houserules` use; it prints nothing until HR-001 is fixed.",
        "For a change to `update`, run `init` and then `update` in the same scratch repository and confirm that only kit-owned files changed."
      ],
      "tags": [
        "live-run",
        "cli",
        "scratch"
      ],
      "source": {
        "date": "2026-08-31",
        "by": "user",
        "ref": "orchestrator brief of 2026-08-31"
      },
      "see": [
        "process.live-run-before-ci"
      ]
    },
    {
      "id": "houserules.node-under-mise",
      "kind": "gotcha",
      "area": "global",
      "summary": "`node` and `npm` are on PATH only under `mise exec --` (`mise exec -- npm test`); in a directory without `mise.toml`, mise resolves no tools at all.",
      "body": [
        "For a scratch directory, put `$(dirname \"$(mise which node)\")` on PATH before you run `node` or `npm` there.",
        "The shipped wrappers `tools/kb.sh` and `tools/backlog.sh` use `node` from PATH and fall back to `mise exec node` on their own."
      ],
      "tags": [
        "mise",
        "node",
        "environment"
      ],
      "source": {
        "date": "2026-08-31",
        "by": "controller",
        "ref": "session of 2026-08-31: mise could not exec npm in a scratch directory"
      }
    },
    {
      "id": "houserules.rename-map",
      "kind": "decision",
      "area": "template",
      "summary": "Relative to tag-pilot: unprefixed agent and skill names, `project-knowledge` skill, `dependency_vetting` for field `a19`, parameterized id prefix.",
      "body": [
        "Agents: `tagpilot-implementer` → `implementer`, `tagpilot-task-reviewer` → `task-reviewer`, `tagpilot-branch-reviewer` → `branch-reviewer`. Skills: `tagpilot-orchestrating` → `orchestrating`, `tagpilot-knowledge` → `project-knowledge`.",
        "Backlog ids: `TP-` → `--id-prefix` (default `WI`, `HR` in this repository). The install stamp is `.houserules.json`.",
        "The kit was named lorekit until 2026-08-31; docs/design.md §5.1 records the rename."
      ],
      "tags": [
        "tag-pilot",
        "names",
        "migration"
      ],
      "source": {
        "date": "2026-08-31",
        "by": "docs",
        "ref": "docs/design.md §4, §5.1, §5.4"
      }
    },
    {
      "id": "houserules.payload-runs-on-builtins",
      "kind": "invariant",
      "area": "template",
      "summary": "The payload in `template/` runs on Node built-ins and POSIX shell only: no dependency, no build step; the CLIs work in a fresh clone before any install.",
      "body": [
        "The SessionStart hook and the CI gates run where nothing is installed; a runtime dependency would break both (docs/design.md §2, option B).",
        "This repository's devDependencies (vitest) serve its own tests only and never enter `template/`."
      ],
      "tags": [
        "payload",
        "dependencies",
        "portability"
      ],
      "source": {
        "date": "2026-08-30",
        "by": "docs",
        "ref": "docs/design.md §2–3"
      }
    }
  ]
}
