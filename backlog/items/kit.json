{
  "$schema": "../schema.json",
  "section": "kit",
  "heading": "Kit",
  "title": "Kit machinery and payload",
  "spec": "docs/design.md",
  "items": [
    {
      "id": "HR-001",
      "type": "bug",
      "milestone": null,
      "status": "done",
      "title": "Run the CLI through a symlinked path (npm .bin link, node_modules/<pkg> under pnpm)",
      "body": [
        "npm installs `bin` as a symlink `node_modules/.bin/houserules -> ../houserules/bin/houserules.mjs`. Node resolves `import.meta.url` through the symlink but keeps `process.argv[1]` as the `.bin` path, so the entry guard in `bin/houserules.mjs` never matches and the CLI exits 0 without output.",
        "Fix: compare against `realpathSync(process.argv[1])` in the guard; apply the same idiom in `template/tools/kb.mjs` and `template/tools/backlog.mjs`. Regression test: run the bin through a symlink and expect the manifest.",
        "Verified live on 2026-08-31 with `npm exec --package=git+file://<clone> -- houserules files`: 0 lines, exit 0; the direct path prints the manifest.",
        "Probe of 2026-09-01 under pnpm 11.18.0 (`pnpm add --save-exact git+file://<clone>` in a scratch project): `pnpm exec houserules files` and `pnpm dlx git+file://<clone> files` print the manifest, because pnpm's bin shim execs the real path under `node_modules/.pnpm/`; `node node_modules/houserules/bin/houserules.mjs files` through the package symlink prints nothing and exits 0. The defect is independent of the package manager."
      ],
      "batch": 2,
      "tags": [
        "cli",
        "npm",
        "pnpm",
        "symlink"
      ]
    },
    {
      "id": "HR-002",
      "type": "fix",
      "milestone": null,
      "status": "done",
      "title": "Report invalid JSON in knowledge and backlog files as a usage error",
      "body": [
        "`loadBase` and `loadBacklog` let `JSON.parse` errors escape as stack traces (`SyntaxError: Expected property name or '}' in JSON at position 1`); `tools/kb.sh render` inside `init` then dies with the raw trace.",
        "Fix: `readJson` raises a `UsageError` that names the file and the parse message, as `audit --report` already does for a malformed report. `init` keeps failing with a non-zero exit; the init test `lets a render failure on broken project data propagate as a real error` stays valid."
      ],
      "batch": 2,
      "tags": [
        "tools",
        "errors"
      ]
    },
    {
      "id": "HR-003",
      "type": "process",
      "milestone": null,
      "label": "blocked: tag-pilot PR #42",
      "status": "done",
      "title": "Port process.evals-rerun with its co-change check and evals record from tag-pilot",
      "body": [
        "tag-pilot `fc3241b` (2026-08-31) adds the rule `process.evals-rerun`: re-run every `.claude/evals/` scenario whenever the implementer or task-reviewer template or a scenario changes; a `co-change` check fails the audit until `.claude/evals/record.json` changes with them; the record keys runs by the templates' git blob ids. The orchestrating skill gains a 'Template evaluation' procedure and the branch reviewer compares the record's blob ids with HEAD.",
        "Blocked until PR #42 is merged (owner instruction). Then port it to `template/` (knowledge entry, orchestrating skill bullet, branch-reviewer check, and the `kb.mjs` co-change lines if the drift holds them) and diff the remaining drift."
      ],
      "tags": [
        "tag-pilot",
        "evals",
        "parity"
      ],
      "batch": 4
    },
    {
      "id": "HR-004",
      "type": "chore",
      "milestone": null,
      "status": "done",
      "title": "Switch this repository to pnpm and set the workspace up like tag-pilot",
      "body": [
        "Owner ruling of 2026-09-01 (docs/design.md §5.7): use pnpm only, never npm or npx.",
        "Scope: exact `node` and `pnpm` pins in `mise.toml` written by `mise use`; `pnpm-lock.yaml` replaces `package-lock.json`; `pnpm-workspace.yaml` for the pnpm settings; the mise tasks `setup`, `test`, `lint`, and `audit` as in tag-pilot; README, knowledge entries, and the development commands name pnpm. `template/` stays dependency-free.",
        "The batch 2 spec fixes the file list and settles the commitlint commit-msg hook."
      ],
      "batch": 2,
      "tags": [
        "pnpm",
        "workspace",
        "mise"
      ]
    },
    {
      "id": "HR-005",
      "type": "fix",
      "milestone": null,
      "status": "done",
      "title": "Capture the render child's stderr in init instead of inheriting it",
      "body": [
        "`install` in `bin/houserules.mjs` spawns `tools/kb.mjs render` with an inherited stderr, so the init test on broken project data prints one usage-error line into the suite output (tests/init.test.mjs, 'lets a render failure on broken project data propagate as a real error').",
        "Fix: spawn with `stdio: ['ignore', 'pipe', 'pipe']` and put the child's stderr into the thrown error; the test asserts the message and the suite output stays silent.",
        "Source: task 3 re-review and the batch 2 branch review (2026-09-01)."
      ],
      "tags": [
        "cli",
        "tests"
      ],
      "batch": 3
    },
    {
      "id": "HR-006",
      "type": "process",
      "milestone": null,
      "status": "done",
      "title": "Enforce the commit body line limit in the commits check",
      "body": [
        "The `commits` check of `tools/kb.sh audit` tests the subject pattern and `body_absent` only; two controller commits with 190- and 227-character body lines passed the batch 2 audits as '9 commits checked'. Only the commit-msg hook catches it, for commits made after it was installed.",
        "Fix: add the optional key `body_line_max` (integer, minimum 1) to `$defs.check` in `knowledge/schema.json` and `template/knowledge/schema.json`; in the `commits` runner of `template/tools/kb.mjs`, fail a commit with a body line longer than the limit; regression tests for 100 and 101 characters and a `checkShape` case; `update --dir .`.",
        "Ruled 2026-09-01 (docs/design.md §5.8): the same task sets `body_line_max: 100` on `process.conventional-commits` in `knowledge/process.json` and `template/knowledge/process.json`. Source: batch 2 branch review, violated_rules."
      ],
      "tags": [
        "tools",
        "audit",
        "standing"
      ],
      "batch": 3
    },
    {
      "id": "HR-007",
      "type": "chore",
      "milestone": null,
      "status": "done",
      "title": "Agent template and deliverables refinements from the batch 2 retrospective",
      "body": [
        "Deliverables schema (`template/.claude/schemas/deliverables.json` and the root copy): add an optional integer `exit` to `$defs.run`; the implementer template's Report section names it for every run whose exit code is evidence, and `output` stays verbatim (batch 2: exit codes were written into `output` as prose).",
        "Implementer template `coverage`: 'one measure per target whenever the project has a coverage gate and the full suite ran, so the batch keeps a baseline; null only when the suite did not run' (batch 2 task 1 left it null and a fix round repaired it).",
        "Orchestrating skill, dispatch protocol: a brief names no version number for a tool, action, or package (security-hygiene.exact-pins); it names the verification the implementer runs and shows placeholders such as `jdx/mise-action@<current major>` (batch 2 task 1 brief carried a stale `@v2`).",
        "Source: batch 2 branch review, template_defects 2-4."
      ],
      "tags": [
        "template",
        "agents",
        "schemas"
      ],
      "batch": 3
    },
    {
      "id": "HR-008",
      "type": "feat",
      "milestone": null,
      "status": "done",
      "title": "Audit report-field checks over a workspace of task reports",
      "body": [
        "A branch has no single report, so the four report-field rows (deliverables-json, knowledge-first, dependency-vetting, verify-current-docs) come back `skipped` from `tools/kb.sh audit` and the branch reviewer judges them by hand from the task reports.",
        "Fix: accept `--report` more than once, or add `--workspace <dir>` that reads every `task-*-report.json`; a report-field check passes when every report whose changed files trigger it sets the field, and names the offending report otherwise. Source: batch 2 branch review, template_defects 5."
      ],
      "tags": [
        "tools",
        "audit"
      ],
      "batch": 3
    },
    {
      "id": "HR-009",
      "type": "bug",
      "milestone": null,
      "status": "done",
      "title": "Diff the audit range from the merge base",
      "body": [
        "`changedFiles` in `template/tools/kb.mjs` diffs `base..head`, so the pull-request audit (`.github/workflows/ci.yml`) sees unrelated files when main moves after the branch was cut. Inherited from tag-pilot's knowledge.yml.",
        "Fix: `git diff --name-only --diff-filter=ACMR --merge-base <base> <head>` for the files, `git log <base>..<head>` for the commits; a test with a moved base. Source: batch 2 branch review, recommendations."
      ],
      "tags": [
        "tools",
        "audit",
        "ci"
      ],
      "batch": 3
    },
    {
      "id": "HR-010",
      "type": "fix",
      "milestone": null,
      "status": "done",
      "title": "Report an unreadable .houserules.json as a usage error",
      "body": [
        "`install` parses the stamp with raw `JSON.parse(readFileSync(markerPath))`; an unreadable `.houserules.json` escapes as a SyntaxError stack trace. `tests/init.test.mjs` (the test \"propagates an unreadable marker, which is a defect and not a usage error\") pins the current behavior until this item.",
        "Fix: reuse `readJson` from `template/tools/lib/json-store.mjs` (it raises `UsageError` with `<path>: invalid JSON (...)`; the bin already imports from that module) and rewrite the pinning test the way HR-005 rewrote its predecessor.",
        "Source: batch 3 task 1 review (2026-09-01); the branch review filed the missing item as its critical finding."
      ],
      "tags": [
        "cli",
        "errors"
      ],
      "batch": 4
    },
    {
      "id": "HR-011",
      "type": "fix",
      "milestone": null,
      "status": "done",
      "title": "Carry the git stderr in the no-merge-base usage error",
      "body": [
        "`gitDiff` in `template/tools/kb.mjs` reports every `git diff` failure as `no merge base between \"<base>\" and \"<head>\"` and discards the child stderr, so a different failure (a missing object in a shallow clone, repository corruption) is mislabeled and its diagnostic lost.",
        "Fix: pipe the child stderr and include it in the `UsageError`, or use the no-merge-base text only when the stderr indicates it; sync the root copy; one test.",
        "Deferred from batch 3 with the branch review’s reason: `rev` pre-validates both refs, so the remaining failure modes are exotic. Source: batch 3 branch review, minor 2 (2026-09-01)."
      ],
      "tags": [
        "tools",
        "audit",
        "errors"
      ],
      "batch": 4
    },
    {
      "id": "HR-012",
      "type": "chore",
      "milestone": null,
      "status": "done",
      "title": "Dispatch-protocol bullets: a brief carries the spec, a dispatch carries changed templates",
      "body": [
        "Two additions to the orchestrating skill’s dispatch protocol (`template/.claude/skills/orchestrating/SKILL.md`, kit-owned, then `update --dir .`): a brief names every test and gate its spec task lists, verbatim or by pointer (batch 3 task 3’s brief dropped the seed-checkBase test and the gap cost a review round); when a batch edits an agent template, the dispatch message carries the changed instruction verbatim, because templates load at session start (batch 3’s branch review ran on the pre-branch template).",
        "Consider seeding `process.brief-carries-the-spec` (added to the root knowledge at the batch 3 close) into `template/knowledge/process.json` in the same task.",
        "Source: batch 3 branch review, template_defects 1 and 3 (2026-09-01)."
      ],
      "tags": [
        "template",
        "skills",
        "dispatch"
      ],
      "batch": 4
    },
    {
      "id": "HR-013",
      "type": "feat",
      "milestone": null,
      "status": "done",
      "title": "A migration skill: move a pre-existing codebase’s knowledge into the houserules style",
      "body": [
        "Owner request of 2026-09-01: when houserules is installed into a codebase that already has knowledge (CLAUDE.md prose, docs, wikis, comments, ad-hoc rules files), a skill in the payload explains how to migrate it: inventory the sources, split rule/invariant/gotcha/procedure/decision/history, write entries per the schema (summary is the rule, ≤160; body why/how/exceptions; source with date and by; verify paths; check where deterministic), extend areas, park the leftovers, delete the migrated originals, and gate with tools/kb.sh check plus render.",
        "Shape to design in the batch spec: a kit-owned skill (template/.claude/skills/, overwritten by update) that the README’s install-to-existing section points to; likely with a worked example and the knowledge-base.* rules by pointer.",
        "Candidate for batch 5 with its own spec gate."
      ],
      "tags": [
        "template",
        "skills",
        "migration",
        "adoption"
      ],
      "batch": 5
    },
    {
      "id": "HR-014",
      "type": "fix",
      "milestone": null,
      "status": "done",
      "title": "Read settings.json through readJson in the bin",
      "body": [
        "`mergeSettings` in `bin/houserules.mjs` still parses `.claude/settings.json` by hand and wraps the failure as `not readable JSON (...)`, a second wording for the failure class the stamp path now reports as `invalid JSON` through `readJson`.",
        "Fix: `settings = readJson(path)` and drop the catch; the existing test asserts exit 2 and the file name only. Decide the one semantic change: an unreadable (not just invalid) settings.json then propagates as a plain Error, like the stamp.",
        "Source: batch 4 task 1 review, minor (2026-09-01); deferred out of HR-010 because of that semantics decision."
      ],
      "tags": [
        "cli",
        "errors"
      ],
      "batch": 5
    },
    {
      "id": "HR-015",
      "type": "fix",
      "title": "Commit-msg hook rejects trailers; ship .githooks in the payload",
      "body": [
        "The hook runs commitlint only, so a harness-injected Co-Authored-By trailer reached a batch 4 commit and was caught by hand (task 3); security-hygiene.no-coauthor is enforced only post-hoc by the audit.",
        "Fix: a trailer gate in .githooks/commit-msg before commitlint (grep -qiE for Co-Authored-By → one error line, exit 1). Consider shipping .githooks in the kit payload (KIT_OWNED) so consumers get the gate; this repository has no template copy of the hook today.",
        "Source: batch 4 branch review, template_defects 1 (2026-09-02)."
      ],
      "tags": [
        "hooks",
        "template"
      ],
      "milestone": null,
      "status": "done",
      "batch": 6
    },
    {
      "id": "HR-016",
      "type": "feat",
      "title": "A structured live_run field in task reports, with a recipe check",
      "body": [
        "Live-run evidence has no structured field, so a missing scratch recipe is findable only in prose (batch 4 task 2). Add an optional live_run array (command, output, exit) to $defs.taskReport in both deliverables schema copies; give houserules.live-run-recipe a report-field check at warn on live_run; the implementer template names the field.",
        "Source: batch 4 branch review, template_defects 2 (2026-09-02)."
      ],
      "tags": [
        "schemas",
        "template",
        "audit"
      ],
      "milestone": null,
      "status": "done",
      "batch": 7
    },
    {
      "id": "HR-017",
      "type": "feat",
      "title": "A mode field on tdd cycles: natural, mutation, or reconstructed",
      "body": [
        "A tdd cycle cannot say how it was produced; batch 4 task 4 spent two report rounds wording the difference between a natural RED, a mutation proof, and a post-commit reconstruction. Add an optional mode enum (natural | mutation | reconstructed) to $defs.tddCycle in both schema copies and one implementer-template sentence: set mode when the cycle is not a natural pre-commit RED.",
        "Source: batch 4 branch review, template_defects 3 (2026-09-02)."
      ],
      "tags": [
        "schemas",
        "template"
      ],
      "milestone": null,
      "status": "done",
      "batch": 7
    },
    {
      "id": "HR-018",
      "type": "fix",
      "title": "Co-change evidence names a record-only change plainly",
      "body": [
        "When the then file itself matches the if glob, the co-change evidence is circular (\"record.json changed with record.json\"). In template/tools/kb.mjs: when the only if match is the then path, print \"only <then> changed; the co-change is satisfied by definition\"; otherwise exclude the then path from the listed if matches. Behavior unchanged; one test.",
        "Source: batch 4 branch review, template_defects 4 (2026-09-02)."
      ],
      "tags": [
        "tools",
        "audit"
      ],
      "milestone": null,
      "status": "done",
      "batch": 6
    },
    {
      "id": "HR-019",
      "type": "bug",
      "title": "Area glob matching misses dot-segments under **",
      "body": [
        "node:path matchesGlob excludes dot-segments under **: matchesGlob('template/.claude/skills/migrating-knowledge/SKILL.md', 'template/**') returns false (verified live in the batch 5 branch review). Every batch 5 audit ran without the template area: houserules.payload-runs-on-builtins dropped out of the audit package, and the rules loader never serves .claude/rules/template.md for template/.claude/** files.",
        "Fix in template/tools/kb.mjs areaFiles (matchesGlob, ~line 106): wrap matchesGlob with a dot-inclusive variant, or a small globToRegExp with dot semantics. RED first: areasFor(['template/.claude/agents/implementer.md'], areas) must include 'template'. The fix ships in the payload; target repositories inherit it via update.",
        "Source: batch 5 branch review, template_defects 1 (2026-09-02)."
      ],
      "tags": [
        "tools",
        "audit",
        "areas"
      ],
      "milestone": null,
      "status": "done",
      "batch": 6
    },
    {
      "id": "HR-020",
      "type": "bug",
      "title": "mergeSettings crashes on valid non-object JSON in settings.json",
      "body": [
        "A .claude/settings.json containing null, a string, or a number passes readJson and then crashes with a plain TypeError stack at settings.hooks ??= {}. Pre-existing behavior, not regressed by batch 5 task 2.",
        "Cover it with the CLI's one-line UsageError convention. RED first: a scratch settings.json containing null.",
        "Source: batch 5 branch review, recommendation 2 (2026-09-02)."
      ],
      "tags": [
        "cli",
        "settings"
      ],
      "milestone": null,
      "status": "done",
      "batch": 6
    },
    {
      "id": "HR-021",
      "type": "feat",
      "title": "A verdict.text field for re-review prose; open lists only prior findings",
      "body": [
        "verdict.open is a bare textList; batch 5 task-1-review-r1.json used it for a status sentence plus restatements of both new_breakage items, so state 'findings-remain' read as prior findings open when all eight were addressed and only new breakage remained.",
        "Add an optional verdict.text for prose to $defs.reReview in .claude/schemas/deliverables.json (root and template). State in task-reviewer.md that open lists only prior findings still unaddressed and that new breakage lives only in new_breakage.",
        "Changing task-reviewer.md triggers process.evals-rerun: re-run every scenario and append the run set to .claude/evals/record.json.",
        "Source: batch 5 branch review, template_defects 2 (2026-09-02)."
      ],
      "tags": [
        "template",
        "schema",
        "reviews"
      ],
      "milestone": null,
      "status": "done",
      "batch": 7
    },
    {
      "id": "HR-022",
      "type": "bug",
      "title": "The stamp read crashes on a non-object .houserules.json",
      "body": [
        "install reads the marker with 'existsSync(markerPath) ? readJson(markerPath) : { idPrefix: prefix }' and then touches marker.idPrefix; a .houserules.json holding null (or a string, a number, a boolean, an array) passes readJson and crashes with a raw stack at bin/houserules.mjs:185. Verified live at batch 6 HEAD 8175928: first init exit 0, 'printf null > .houserules.json', second init exit 1 with the stack.",
        "Fix as HR-020's sibling: reject a non-object marker with the one-line UsageError convention ('<path>: not a JSON object'), RED first; the existing HR-010 stamp tests stay green.",
        "Source: batch 6 task 3 review, minor 2 (2026-09-02)."
      ],
      "tags": [
        "cli",
        "stamp"
      ],
      "milestone": null,
      "status": "done",
      "batch": 7
    },
    {
      "id": "HR-023",
      "type": "fix",
      "title": "Re-review dispatches carry the round-0 block; audits refuse a missing --ids",
      "body": [
        "The batch 6 task 1 round-1 re-review dispatch omitted the Backlog: line and the --ids list, so task-1-audit-r1.json ran a narrowed package (27 rows; houserules.live-run-recipe and houserules.node-under-mise dropped). The reviewer caught it; round 2 was dispatched correctly, but nothing forces the block.",
        "Fix: the orchestrating skill's re-review dispatch bullet requires the identical Knowledge:/Backlog:/--ids block as the round-0 dispatch; the task-reviewer template refuses to run an audit without --ids when the task's id list is non-empty.",
        "Changing task-reviewer.md triggers process.evals-rerun: re-run every scenario and append the run set to .claude/evals/record.json.",
        "Source: batch 6 branch review, template_defects 1 (2026-09-02)."
      ],
      "tags": [
        "template",
        "agents"
      ],
      "milestone": null,
      "status": "done",
      "batch": 7
    },
    {
      "id": "HR-024",
      "type": "fix",
      "title": "Stamp an empty-range audit as vacuous",
      "body": [
        "An audit with base == head (batch 6 task 1's report-only round 2) succeeds with vacuous deterministic rows ('0 commits checked', '0 files checked', 'not triggered') that read as positive evidence; the round-2 reviewer had to annotate them as vacuous by hand.",
        "Fix in template/tools/kb.mjs: when the range holds no commits, set \"empty_range\": true in the audit summary and prefix the deterministic evidence strings with 'empty range:'. RED first.",
        "Source: batch 6 branch review, template_defects 2 (2026-09-02)."
      ],
      "tags": [
        "tools",
        "audit"
      ],
      "milestone": null,
      "status": "done",
      "batch": 7
    },
    {
      "id": "HR-025",
      "type": "test",
      "title": "Pin the two SEED_ONCE deliverables schema copies in sync",
      "body": [
        "tests/dogfood.test.mjs pins every KIT_OWNED root copy byte-for-byte, but .claude/schemas/deliverables.json is SEED_ONCE: update never writes it, and its root and template copies are hand-synced. They differ only in the backlogId pattern (HR- vs WI-) by design.",
        "Batch 7 hand-synced four new fields across both copies on manual diligence alone. Add a test that compares the copies modulo the known backlogId pattern difference, so a drift fails the suite.",
        "Source: batch 7 task 3 review, controller note (2026-09-02)."
      ],
      "tags": [
        "tests",
        "schemas"
      ],
      "milestone": null,
      "status": "open",
      "batch": 8
    },
    {
      "id": "HR-026",
      "type": "fix",
      "title": "The audit package admits area-matched entries that carry a check",
      "body": [
        "The package builder (template/tools/kb.mjs:709) admits only standing entries and area-matched rule/invariant kinds, so the report-field check on houserules.live-run-recipe (kind procedure, area cli) never joins a range-derived package: the batch 7 branch audit carried no live_run row though the plan promised it. Task audits carried it only via --ids.",
        "Fix: admit any area-matched entry that carries a check (e.g. e.check && areas.includes(e.area)), RED first with a regression test that a checked procedure joins the package; template first, root synced. Damage is minor today because the schema makes live_run required, which fails validate harder than the warn row.",
        "Deferred out of batch 7 by the controller: audit-machinery semantics deserve full review rounds, not a post-branch-review patch. Source: batch 7 branch review, minor 1 + template_defects 1 (2026-09-02)."
      ],
      "tags": [
        "tools",
        "audit"
      ],
      "milestone": null,
      "status": "open",
      "batch": 8
    },
    {
      "id": "HR-027",
      "type": "fix",
      "title": "The stamp guard validates idPrefix as a string",
      "body": [
        "readJsonObject accepts any plain object, so a .houserules.json holding an idPrefix that is a number passes the guard and the number flows into id generation. Owner-candidate: decide whether the stamp is fully validated or the object guard suffices.",
        "Source: batch 7 task 1 review, reviewer note (2026-09-02)."
      ],
      "tags": [
        "cli",
        "stamp"
      ],
      "milestone": null,
      "status": "open",
      "batch": 8
    },
    {
      "id": "HR-028",
      "type": "fix",
      "title": "install validates the stamp before writing kit-owned files",
      "body": [
        "install rewrites every KIT_OWNED file before it reads the stamp, so a corrupt .houserules.json exits 2 with the target already updated. Pre-existing since batch 4; same order as the invalid-JSON path. Owner-candidate: decide whether the stamp read moves ahead of the writes.",
        "Source: batch 7 task 1 review, reviewer note (2026-09-02)."
      ],
      "tags": [
        "cli",
        "stamp"
      ],
      "milestone": null,
      "status": "open",
      "batch": 8
    },
    {
      "id": "HR-029",
      "type": "chore",
      "title": "The dependency-add eval query names its payload collision",
      "body": [
        "The query names the project's tooling package, which collides with houserules.payload-runs-on-builtins when run in this repository, forcing a deviation disclosure every run; the judge cannot tell a designed trap from an accidental ambiguity.",
        "Fix: name the repository's own devDependencies in the query, or add the collision to expected_behavior as an explicit judged criterion. Changing a scenario triggers process.evals-rerun.",
        "Source: batch 7 branch review, template_defects 2 (2026-09-02)."
      ],
      "tags": [
        "evals",
        "template"
      ],
      "milestone": null,
      "status": "open",
      "batch": 8
    },
    {
      "id": "HR-030",
      "type": "chore",
      "title": "implementer.md's Report section lists one field per line",
      "body": [
        "After live_run and mode landed, the Report field list is one long single sentence; each contract change re-inflates it and makes template diffs hard to review. Restructure as one line per field, wording unchanged.",
        "Changing implementer.md triggers process.evals-rerun; schedule with the next template batch.",
        "Source: batch 7 branch review, template_defects 4 (2026-09-02)."
      ],
      "tags": [
        "template",
        "agents"
      ],
      "milestone": null,
      "status": "open",
      "batch": 8
    },
    {
      "id": "HR-031",
      "type": "fix",
      "title": "One idPrefix predicate serves the flag and the stamp",
      "body": [
        "install validates the --id-prefix flag with /^[A-Z][A-Z0-9]{0,7}$/ but the stamp guard (HR-027) accepts any non-empty string, so a hand-edited .houserules.json can carry a prefix init would refuse (' ', 'wi', 'TOOLONGPREFIX') and the next line writes it back. Two validators for one concept in one function.",
        "Owner-candidate: unify on the flag's regex as one shared predicate with one message, or rule that the stamp's looser contract is intended and record why. The batch 8 spec scoped HR-027 to the non-empty-string check, so the divergence is a choice to make, not an accident to patch.",
        "Source: batch 8 task 3 review, minor 4 (2026-09-02)."
      ],
      "tags": [
        "cli",
        "stamp"
      ],
      "milestone": null,
      "status": "open"
    }
  ]
}
